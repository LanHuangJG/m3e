For production builds, use the minified files to ensure optimal load performance.
. To use it directly in a browser without a bundler, use a module script similar to the following.
( ), which documents the properties, attributes, slots, events and CSS custom properties of each component.
You can explore the API below, or integrate the manifest into your own tooling.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js" } } </script>
.density-3 { --md-sys-density-scale: -3; } /* Additional classes omitted for brevity */
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js" } } </script>
This component participates in form submission as a
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js" } } </script>
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js" } } </script>
Icons slots have to ensure icons are always hidden from assistive technologies.
Different browsers interpret date strings in inconsistent ways, especially when the string resembles an ISO date but does not include a time or timezone. To ensure predictable behavior across environments, uses a consistent, standards-aligned parsing model instead of relying on the browser's built-in rules.
When receives a date string, it automatically detects whether the value matches one of the ISO-8601 formats that can be parsed safely and unambiguously:
— date and time with an explicit timezone offset
Strings in these formats are converted into local objects without unexpected timezone shifts. This prevents issues such as date-only values drifting into the previous or next day when interpreted as UTC. If a string does not follow one of these ISO-compatible patterns, parsing may be ambiguous or unsupported. For the most reliable results, use one of the formats above.
Use the attribute to control the selected state of a . When clicked, a cancellable event is emitted followed by an and event. To cancel selection change, call during .
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js", "@m3e/web/core/layout": "/node_modules/@m3e/web/dist/core-layout.js" } } </script>
The following illustrates applying density to both the and 's panel via use of the attribute.
Options are given ARIA , indicating that each item represents a selectable choice within a listbox context. This role enables assistive technologies to interpret and announce the options appropriately, supporting accessible navigation and selection behavior.
The attribute reflects whether an option is currently selected. When an option is selected, is exposed to assistive technologies; otherwise, it is .
The attribute is applied to the element to indicate which option is currently active within the listbox. Instead of moving DOM focus to each option, the select retains focus while updating to reference the of the focused . This approach preserves focus management and enables assistive technologies to announce the active option during keyboard navigation, ensuring accessible and predictable interaction.
The attribute reflects the current state of the listbox popup. When the listbox is open, is set on the element; when collapsed, it is set to . This dynamic state enables screen readers to announce whether the listbox is expanded or collapsed, supporting accessible navigation and interaction.
Because is designed for single-item selection, you should avoid placing additional interactive elements—such as buttons, checkboxes, or toggles—inside . Nesting interactive controls within options disrupts expected navigation and interferes with screen reader behavior.
You also need a module script for due to it being a dependency.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js", "@m3e/web/core/anchoring": "/node_modules/@m3e/web/dist/core-anchoring.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js", "@m3e/web/option": "/node_modules/@m3e/web/dist/option.js" } } </script>
NOTE: this page is presented in a nested .
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js" } } </script>
There are two appearance variants: (default) and . Use the attribute to change the appearance.
The following illustrates applying density to both the and variants.
The attribute can be used to set the view that will show up when the calendar first opens. It can be set to , , or ; by default it will open to view.
Use the property to specify a function used to determine whether a date has a highlighted look and feel. You can use the , , , , and CSS properties to style special dates.
uses the Gregorian calendar, and all date formatting is handled by the browser's APIs based on the active locale.
The calendar is fully accessible and exposes clear semantics for assistive technologies. The header contains navigation controls, each with an accessible label that can be customized through attributes. The calendar grid is implemented using , with each date cell using and containing a focusable element with . Selected dates and range boundaries are indicated using , while today's date is marked with . Each cell includes a visually hidden, locale-aware full date string for screen readers, while the visible text shows only the day number. Day-of-week headers use abbreviated labels but include visually hidden full names (such as “Monday”) to ensure clear, unambiguous announcements.
— Accessible label for the button that navigates to the previous month.
— Accessible label for the button that navigates to the next month.
— Accessible label for the button that navigates to the previous year.
— Accessible label for the button that navigates to the next year.
— Accessible label for the button that navigates to the previous 24-year range.
— Accessible label for the button that navigates to the next 24-year range.
The component provides a modular layout for structured content blocks. It exposes the following named slots:
(FACE), contributing its state to the form data. The attribute defines the key under which the component's value is submitted in a form. When checked and not disabled, (defaults to ) is submitted.
The provides a single slot, , to present an icon before a chip's label. Trailing icons are not supported for this chip type.
The component honors the state (on the chip set itself) as a
Use the to open a . This component should be nested inside a clickable element and be associated with a picker by setting the for attribute to the id of the picker to open.
<m3e-list> <m3e-list-item>Item 1</m3e-list-item> <m3e-divider></m3e-divider> <m3e-list-item>Item 2</m3e-list-item> <m3e-divider></m3e-divider> <m3e-list-item>Item 3</m3e-list-item> </m3e-list>
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js", "@m3e/web/core/anchoring": "/node_modules/@m3e/web/dist/core-anchoring.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js" } } </script>
Slots have to ensure icons are always hidden from assistive technologies.
As illustrated above, items are specified using the component. Use the default slot to specify an item's label and use the slot to specify an icon.
In addition, you can use the slots to specify an alternate icon when selected. When using the to present icons, the icon will automatically be filled when an item is selected.
Items can also behave like links by specifying a URL using the attribute. You can also specify , and similar to native anchor tags.
Items are given the ARIA role , indicating they are clickable elements that trigger actions and is used to convey the selected state.
When disabling using either the or attribute, is used to convey to assistive technologies that the item is disabled. If disabled using , is retained to convey that the item continues to be interactive.
When is specified, the ARIA role is used instead of to convey the item provides an interactive reference to a resource. In addition, the component presents an internal anchor, hidden from assistive technologies, used to invoke native browser features such a context menu options and tooltips.
Focus is managed on the container using , and the currently active item is referenced via , which points to the ID of the focused . This enables roving focus without shifting DOM focus, ensuring accessible keyboard navigation and consistent screen reader feedback.
Disabling an item sets signaling to assistive technologies that the item is not interactive.
Pagination buttons are intentionally excluded from the tab order and strong focus indicators are not supported. This reduces focus noise and streamlines keyboard navigation.
Use the and attributes to provide accessible labels to pagination buttons.
Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat.
Now you can adjust the uploaded image quality, and upgrade your available storage space.
The component is a prominent user interface component that provides consistent access to key actions, navigation, and contextual information at the top of an application screen.
The component provides a structured layout for top-level navigation and contextual information. It exposes the following named slots:
— typically a navigation affordance such as a back arrow or menu button. Positioned at the start of the bar.
— The primary label for the current view or context. Usually a short, expressive heading.
— An optional secondary label that provides additional context or hierarchy beneath the title.
— One or more action buttons aligned to the end of the bar. Common examples include search, bookmark, or overflow menus.
<m3e-app-bar> <m3e-icon-button slot="leading" aria-label="Back"> <m3e-icon name="arrow_back"></m3e-icon> </m3e-icon-button> <span slot="title">Top 10 hiking trails</span> <span slot="subtitle">Discover popular trails</span> <m3e-icon-button slot="trailing" aria-label="Bookmark" variant="tonal"> <m3e-icon name="bookmark" filled></m3e-icon> </m3e-icon-button> </m3e-app-bar>
There are three size variants: (default), , and . Use the attribute to control an app bar's size.
<m3e-app-bar size="medium"> <!-- Content omitted for brevity --> </m3e-app-bar> <m3e-app-bar size="large"> <!-- Content omitted for brevity --> </m3e-app-bar>
A app bar ensures that its primary content — the and optional — remains visually centered within the available horizontal space, regardless of the number or size of elements in the or slots.
This is achieved through layout symmetry, not equal spacing. The title area is anchored to the center of the container, while icons on either side are allowed to vary in number or width without shifting the title's position.
<m3e-app-bar centered> <!-- Content omitted for brevity --> </m3e-app-bar>
An can be attached to a scrollable container using the attribute, which references the container's id. This establishes a scroll contract between the app bar and its content region. As the user scrolls:
The app bar's background becomes elevated — typically via a shadow or surface tone — to signal separation from the content.
When the scroll position returns to the top, the elevation is removed, restoring a flat visual hierarchy.
This behavior enhances contextual clarity, signaling that content is in motion while preserving the app bar's role as a stable anchor.
<div id="scrollContainer"> <m3e-app-bar for="scrollContainer"> <!-- Content omitted for brevity --> </m3e-app-bar> <div class="scroll-item">Scroll down to see the elevation effect</div> </div> <m3e-app-bar centered> <style> #scrollContainer { overflow-y: auto; height: 300px; } m3e-app-bar[for="scrollContainer"] { position: sticky; top: 0; } .scroll-item { height: 400px; display: flex; align-items: center; justify-content: center; } </style>
By default, app bars are given the ARIA to identify them as the site-wide landmark region for branding, navigation, or contextual identity. This role signals to assistive technologies that the app bar serves as the top-level heading for the page or application.
The component enhances an field with a panel of suggested options.
Begin by declaring the element and populating it with a set of elements. Each option represents a selectable value. When the user selects an option, its textual content becomes the input's displayed label and a event is emitted. You can access the selected option via the property, or read its committed value through the property.
To bind the autocomplete panel to an input, use the attribute on , setting it to match the of the target element.
<m3e-form-field> <label slot="label" for="fruit">Choose your favorite fruit</label> <input id="fruit" /> </m3e-form-field> <m3e-autocomplete for="fruit"> <m3e-option>Apples</m3e-option> <m3e-option>Oranges</m3e-option> <m3e-option>Bananas</m3e-option> <m3e-option>Grapes</m3e-option> </m3e-autocomplete>
By default, will filter options using a case insensitive substring match. You can use the attribute to control this behavior. Supported values include: (default), , and . In addition, use the attribute to control whether matching is case-sensitive.
The following example illustrates a case-sensitive prefix match.
<m3e-form-field> <label slot="label" for="fruit">Choose your favorite fruit</label> <input id="fruit" value="Apple" /> </m3e-form-field> <m3e-autocomplete for="fruit" filter="starts-with" case-sensitive> <!-- Options omitted for brevity --> </m3e-autocomplete>
Alternatively, you can provide a callback function using the property to control this behavior. Custom filters do not support automatic text highlighting, because the component cannot determine which parts of the text matched.
The following example shows how to set with logic for a custom case-insensitive prefix match.
<m3e-form-field> <label slot="label" for="fruit">Choose your favorite fruit</label> <input id="fruit" value="Apple" /> </m3e-form-field> <m3e-autocomplete class="custom-filter" for="fruit"> <!-- Options omitted for brevity --> </m3e-autocomplete>
window.addEventListener("DOMContentLoaded", () => { for (const autocomplete of document.querySelectorAll("m3e-autocomplete.custom-filter")) { autocomplete.filter = (option, term) => option.value.toLocaleLowerCase().startsWith(term.toLocaleLowerCase()); } });
Use the attribute to control the text announced and displayed when no options match. The slot can be used to provide custom visual content for the empty state. Alternatively, use the attribute to suppress the empty-state UI entirely.
<m3e-form-field> <label slot="label" for="fruit">Choose your favorite fruit</label> <input id="fruit" value="Pear" /> </m3e-form-field> <m3e-autocomplete for="fruit" no-data-label="No data"> <!-- Options omitted for brevity --> </m3e-autocomplete>
The emits a event that can be used to lazy-load options. The event fires only when the control is enabled and not read-only, under the following conditions:
The receives focus while no options are available. This can be used to load initial options.
The value changes. This can be used to fetch options that match the current term using the event's property.
Use the attribute to control whether the presents a loading UI. The attribute controls the text announced and displayed when enabled and the slot can be used to provide custom visual content for the loading state. Use the attribute to suppress the loading-state UI entirely. will still be announced when hidden.
The following example illustrates lazy loading initial data when the is focused. In addition, a is presented in the slot. See
<m3e-form-field> <label slot="label" for="state">State</label> <input id="state" /> </m3e-form-field> <m3e-autocomplete for="state" class="lazy"> <m3e-loading-indicator slot="loading"></m3e-loading-indicator> </m3e-autocomplete>
const lazy = document.querySelector("m3e-autocomplete.lazy"); lazy.addEventListener("query", () => { if (!lazy.querySelector("m3e-option")) { lazy.loading = true; setTimeout(() => { usStates.forEach((state) => { const option = document.createElement("m3e-option"); option.innerText = state; lazy.appendChild(option); }); lazy.loading = false; }, 2000); } });
The next example performs a search only after the user pauses typing. The loading indicator is not shown while the user is typing and appears only when an actual search request is made. If the user begins typing while a search is in progress, the loading indicator remains visible, the in-flight request is canceled, and a new search is started after debouncing. This example sets to disable built-in filtering.
<m3e-form-field> <label slot="label" for="state">State</label> <input id="state" /> </m3e-form-field> <m3e-autocomplete for="state" filter="none" class="search"> <m3e-loading-indicator slot="loading"></m3e-loading-indicator> </m3e-autocomplete>
const search = document.querySelector("m3e-autocomplete.search"); let debounceTimer = -1; let activeRequest = null; search.addEventListener("query", (e) => { const term = e.detail.term?.trim().toLowerCase(); // User is typing → reset debounce, but do NOT show loading yet clearTimeout(debounceTimer); // If user cleared the field if (!term) { // Cancel any in‑flight request if (activeRequest) activeRequest.abort(); // Reset UI search.querySelectorAll("m3e-option").forEach((x) => x.remove()); search.hideNoData = true; search.loading = false; return; } // Now a real search is happening → show loading search.loading = true; search.hideNoData = false; // If a request is already loading, keep showing loading // but cancel the old request if (activeRequest) activeRequest.abort(); // Start debounce debounceTimer = setTimeout(() => { // Clear old results (so we don't show stale data) search.querySelectorAll("m3e-option").forEach((x) => x.remove()); // Create a cancellable "request" const controller = new AbortController(); activeRequest = controller; // Simulate async search setTimeout(() => { // If canceled, do nothing if (controller.signal.aborted) return; // Populate results usStates .filter((x) => x.toLowerCase().includes(term)) .forEach((state) => { const option = document.createElement("m3e-option"); option.innerText = state; search.appendChild(option); }); search.loading = false; activeRequest = null; }, 2000); }, 300); // debounce delay });
By default, accepts any value typed into the input field, regardless of whether it matches a listed option. To enforce selection from the available options, use the attribute.
When is set, the autocomplete behaves as follows:
and types a new value, then closes the panel without selecting anything, the input resets to an empty string.
If the user opens the panel with a
and types a value that does not match that option, then closes the panel without selecting anything, the input resets to the selected option's label.
If the user opens and closes the panel without modifying the input, the existing value is retained.
<m3e-form-field> <label slot="label" for="fruit">Choose your favorite fruit</label> <input id="fruit" value="Apple" /> </m3e-form-field> <m3e-autocomplete for="fruit" required> <!-- Options omitted for brevity --> </m3e-autocomplete>
Use the attribute to control whether the first option will automatically be activated.
<m3e-form-field> <label slot="label" for="fruit">Choose your favorite fruit</label> <input id="fruit" /> </m3e-form-field> <m3e-autocomplete for="fruit" auto-activate> <!-- Options omitted for brevity --> </m3e-autocomplete>
The pairs with to surface suggestions that users can select and convert into chips. See
<m3e-form-field> <label slot="label" for="fruit">Choose your favorite fruits</label> <m3e-input-chip-set aria-label="Enter favorite fruits"> <input id="fruit" slot="input" placeholder="Add fruit..." /> </m3e-input-chip-set> </m3e-form-field> <m3e-autocomplete for="fruit"> <!-- Options omitted for brevity --> </m3e-autocomplete>
Form fields and autocomplete panels are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Both support negative density scales down to -3, with each step reducing the visual height by one density unit.
<m3e-form-field class="density-3"> <label slot="label" for="fld">Density -3</label> <input id="fld" /> </m3e-form-field> <m3e-autocomplete for="fld" panel-class="density-3"> <!-- Options omitted for brevity --> </m3e-autocomplete> <!-- Additional fields omitted for brevity -->
The component follows the ARIA interaction pattern. The associated text input receives , while the dropdown panel applies to convey its structure to assistive technologies.
When disabled using the attribute, is used to convey to assistive technologies that an option is disabled.
The and attributes are applied to the element to establish an explicit relationship with the listbox popup. Both attributes reference the of the listbox container, ensuring assistive technologies recognize the connection between the trigger and the controlled content. While indicates that the select governs the visibility and behavior of the listbox, asserts DOM ownership when the listbox is rendered outside the input's subtree.
The component provides built-in ARIA-friendly announcements for loading, no-data, and results updates. These announcements are exposed through a polite region so assistive technologies receive timely, non-interruptive updates as the user types.
A loading announcement is issued whenever the attribute becomes , using the value of .
A no-data announcement is issued when no options match the current term and announces the value of .
A results announcement is issued when one or more options become available and announces the value of . If is a function, it receives the number of matching options and must return a string; if it is a plain string, it is announced as-is.
The component is a reusable identity primitive that displays visual or textual representation with consistent sizing, shape, and typography.
The component accepts multiple content types, allowing it to render textual initials, images, or icons depending on what is provided in its slot.
<m3e-avatar>AB</m3e-avatar> <m3e-avatar> <img src="[YOUR IMAGE URL HERE]" /> </m3e-avatar> <m3e-avatar> <m3e-icon name="person"></m3e-icon> </m3e-avatar>
By default, avatars are not given an ARIA role, indicating that they are treated as neutral, non-semantic elements in the accessibility tree unless explicitly referenced or annotated.
The component is a compact visual indicator for counts, presence, or emphasis that can be attached to icons, buttons, or other components.
There are three size variants for a badge: , (default), and . Use the attribute to control a badge's size.
An can be anchored to another element using the attribute, which references the target element's . This creates a semantic and visual link, allowing the badge to track its anchor even across layout shifts.
To control placement, use the attribute. These values define where the badge appears relative to its anchor:
(default) — Above the anchor, aligned to its end edge (right in LTR, left in RTL)
— Above the anchor, aligned to its start edge (left in LTR, right in RTL)
— Below the anchor, aligned to its start edge
— Below the anchor, aligned to its end edge
— Inline with the anchor, placed before it (left in LTR, right in RTL)
— Inline with the anchor, placed after it (right in LTR, left in RTL)
, adapting automatically to left-to-right and right-to-left layouts. They honor expressive anchoring and layout clarity, even across responsive shifts.
<m3e-button id="btn" variant="filled">Button</m3e-button> <m3e-badge for="btn" position="above-after">AA</m3e-badge> <m3e-badge for="btn" position="above-before">AB</m3e-badge> <m3e-badge for="btn" position="below-before">BB</m3e-badge> <m3e-badge for="btn" position="below-after">BA</m3e-badge> <m3e-badge for="btn" position="before">BE</m3e-badge> <m3e-badge for="btn" position="after">AF</m3e-badge> <m3e-badge for="btn" position="above">A</m3e-badge> <m3e-badge for="btn" position="below">B</m3e-badge>
By default, badges are not given an ARIA role, indicating that they are treated as neutral, non-semantic elements in the accessibility tree unless explicitly referenced or annotated.
Use when attaching a badge to an interactive element.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js", "@m3e/web/core/anchoring": "/node_modules/@m3e/web/dist/core-anchoring.js" } } </script>
The component is a sheet used to show secondary content anchored to the bottom of the screen.
The modal bottom sheet scrim will only overlay the current window.
The bottom sheet is positioned relative the current window.
Use the to present content anchored to the bottom of the screen. Bottom sheets are closed by default. You can open a sheet using a nested inside any clickable element. You can also open a sheet declaratively with the attribute or programmatically with the method. To close a sheet, use a inside a clickable element, remove the attribute, or call the method programmatically.
<m3e-button> <m3e-bottom-sheet-trigger for="bottomSheet"> Open bottom sheet </m3e-bottom-sheet-trigger> </m3e-button> <m3e-bottom-sheet id="bottomSheet"> <m3e-action-list> <m3e-list-action> <m3e-bottom-sheet-action>Google Keep</m3e-bottom-sheet-action> <span slot="supporting-text">Add to a note</span> </m3e-list-action> <m3e-list-action> <m3e-bottom-sheet-action>Google Docs</m3e-bottom-sheet-action> <span slot="supporting-text">Embed in a document</span> </m3e-list-action> </m3e-action-list> </m3e-bottom-sheet>
Bottom sheets emit four lifecycle events that reflect their transition between open and closed states. These events fire whether the sheet is opened programmatically, by a trigger, by keyboard interaction, or through user gestures.
— Fired when the sheet begins opening. This event is cancelable, allowing you to prevent the sheet from opening by calling .
— Fired after the sheet finishes opening and reaches its initial detent.
— Fired when the sheet begins closing. This event is also cancelable, allowing you to intercept or block dismissal (for example, to confirm unsaved changes).
— Fired after the sheet finishes closing and is fully hidden.
These events give you full control over the sheet's lifecycle, including validation, analytics, and custom behaviors during open and close transitions.
. Both share the same anatomy and resizing behavior, but they differ in how they interact with the rest of the page. A standard bottom sheet is non-blocking. It sits above the page without trapping focus or preventing interaction with the underlying UI. A modal bottom sheet blocks interaction with the rest of the page until dismissed. It presents a backdrop (scrim), traps focus, supports autofocus on open, and restores focus to the trigger when closed.
Bottom sheets are standard by default. Add the attribute to to create a modal bottom sheet.
<!-- Trigger omitted for brevity --> <m3e-bottom-sheet id="bottomSheet" modal> <!-- Content omitted for brevity --> </m3e-bottom-sheet>
When opened in modal mode, the bottom sheet behaves like a dialog and takes exclusive control of user interaction. Focus moves into the sheet automatically, and if any element inside the sheet has the attribute, it receives focus first. While the sheet is open, the rest of the document becomes inert, preventing keyboard and assistive-technology users from interacting with content behind the sheet. Page scrolling is also locked to keep the underlying layout stable.
Modal sheets display a scrim behind the sheet. Activating the scrim triggers a cancelable event, allowing the sheet to close unless the you intercept it. When the sheet closes, focus is restored to the trigger that opened it, ensuring users return to their previous position in the page without losing context.
Bottom sheets support an optional drag handle at the top edge of the sheet. The handle consists of two parts:
— a visual affordance that indicates the sheet can be dragged.
— an interactive control used to cycle detents, expand to full height, or close the sheet when no detents are defined.
Add the attribute to to display the drag handle and use the attribute to provide an accessible label (defaults to "Drag handle").
<!-- Trigger omitted for brevity --> <m3e-bottom-sheet id="bottomSheet" modal handle> <!-- Content omitted for brevity --> </m3e-bottom-sheet>
Detents define the vertical positions a bottom sheet can rest at. A sheet may have no detents, a single detent, or multiple detents. Detents control how the sheet resizes, how it responds to drag gestures, and how the handle button behaves.
Detents are declared using the attribute on . Each detent represents a target height the sheet can snap to.
<!-- Trigger omitted for brevity --> <m3e-bottom-sheet id="bottomSheet" modal handle detents="fit half full"> <!-- Content omitted for brevity --> </m3e-bottom-sheet>
. Both define the vertical positions the sheet can snap to, but they serve different purposes.
Named detents are semantic height presets including , , , and that map to familiar bottom-sheet behaviors. They provide predictable, expressive sizing without requiring you to think in pixels or percentages.
Unit-based detents use explicit measurements (percentages or pixels) to give you precise control over the sheet's height. These behave just like named detents but allow for custom layouts and fine-tuned sizing.
You can mix and match named and unit-based detents in any order. The sheet snaps between them based on drag gestures, velocity, and the order in which they are declared.
The initial height defines the sheet's entry point into the interaction model. For example:
Starting at or creates a more immersive modal feel.
When a bottom sheet opens, it snaps to its initial detent. By default, this is the first detent listed in the attribute. You can override this by setting the attribute (zero-based index), which selects the detent the sheet should open to.
In the following example, the sheet opens at instead of .
Open draggable modal bottom sheet with detents at half
<!-- Trigger omitted for brevity --> <m3e-bottom-sheet id="bottomSheet" modal handle detents="fit half full" detent="1"> <!-- Content omitted for brevity --> </m3e-bottom-sheet>
You can also set the initial detent on a using its attribute, allowing each trigger to open the sheet at a different height.
Open draggable modal bottom sheet with detents at half via trigger
<m3e-button> <m3e-bottom-sheet-trigger for="bottomSheet" detent="1"> Open draggable modal bottom sheet with detents at half via trigger </m3e-bottom-sheet-trigger> </m3e-button> <m3e-bottom-sheet id="bottomSheet" modal handle detents="fit half full"> <!-- Content omitted for brevity --> </m3e-bottom-sheet>
The collapsed state always includes the sheet's header. This ensures the sheet remains visible, discoverable, and interactive even at its lowest detent. You can optionally reveal a portion of the content area beneath the header by setting the CSS custom property. This value applies only to the content area and defaults to , meaning the collapsed state shows the header alone.
When the sheet is collapsed, the content area is always marked inert. This applies whenever the sheet's rendered height falls at or below the combined height of the header and the configured peek height. In this state, only the header is interactive; the content cannot receive focus, pointer events, or screen-reader navigation. This prevents users from interacting with partially hidden elements and ensures the collapsed state behaves as a non-interactive preview. The content becomes interactive again only after the sheet expands above the collapsed height.
Open draggable modal bottom sheet with detents at collapsed with custom peek
<!-- Trigger omitted for brevity --> <m3e-bottom-sheet id="bottomSheet" modal handle detents="collapsed fit half full" style="--m3e-bottom-sheet-peek-height: 36px"> <!-- Content omitted for brevity --> </m3e-bottom-sheet>
A bottom sheet is hideable when it can be dismissed by dragging downward past its lowest detent. This allows the sheet to behave like a modal dialog or a temporary surface that can be fully removed from view. You enable this behavior by adding the attribute to .
<!-- Trigger omitted for brevity --> <m3e-bottom-sheet id="bottomSheet" modal handle hideable> <!-- Content omitted for brevity --> </m3e-bottom-sheet>
<!-- Trigger omitted for brevity --> <m3e-bottom-sheet id="bottomSheet" modal handle hideable detents="fit half full"> <!-- Content omitted for brevity --> </m3e-bottom-sheet>
The provides a named slot for supplying custom content within the sheet's draggable header region. When a header is provided, it appears directly below the optional drag handle. The handle, if enabled, is never replaced by custom header content.
If the handle is not enabled, the entire header region—including any slotted header content—remains draggable. This ensures the sheet always preserves a reliable drag surface regardless of whether a handle is present. If dragging is undesired, place header content in the default slot and omit the attribute.
Open hideable modal bottom sheet with detents and header
<!-- Trigger omitted for brevity --> <m3e-bottom-sheet id="bottomSheet" modal handle hideable detents="collapsed fit half full"> <m3e-heading id="sheetTitle" slot="header" variant="title" size="large"> Choose a destination </m3e-heading> <!-- Content omitted for brevity --> </m3e-bottom-sheet>
Standard bottom sheets use , which identifies the sheet as a labeled section of the page. Because they are not modal, they do not trap focus, support autofocus, or block interaction with the rest of the document.
Bottom sheets are typically opened by a trigger element. The primary trigger exposes the correct ARIA relationships so assistive technologies understand that activating it will reveal the sheet. The trigger automatically receives referencing the sheet's , and its attribute is updated as the sheet opens and closes. Standard sheets are also labelled by their primary trigger when that trigger provides an . Secondary triggers do not participate in ARIA ownership and receive no accessibility attributes; they simply open the sheet. Triggers are considered primary by default; use the attribute on to opt out of ARIA ownership when multiple triggers open the same sheet.
Modal bottom sheets use and , which identify the sheet as a blocking interaction surface. Unlike standard sheets, modal sheets trap focus, prevent interaction with the rest of the page, and require an explicit action to dismiss. The sheet must have an accessible name, provided by either or , so assistive technologies can announce its purpose when it opens.
Modal sheets are also opened by a trigger element. The trigger communicates that activating it will open a modal dialog by receiving .
Modal sheets trap focus and keyboard navigation is contained within the sheet while it is open. Tabbing cycles through the sheet's focusable elements, wrapping from the last element back to the first. The rest of the page is made inert, preventing background content from receiving focus or pointer interaction. When the sheet is dismissed, focus returns to the trigger that opened it, preserving user context and aligning with established dialog accessibility patterns.
The and components work together to display a hierarchical navigation trail.
To create a breadcrumb trail, place your items inside an element. Each represents one level in the navigation hierarchy, starting with the broadest level and ending with the most specific. The final item is treated as the current level automatically, while the earlier items describe the path that leads to it.
Breadcrumb items can also behave like links by specifying a URL using the attribute. You can also specify , and similar to native anchor tags. Links are not supported for the last item in a breadcrumb because the current level is not interactive.
<m3e-breadcrumb> <m3e-breadcrumb-item href="https://developer.mozilla.org/en-US/docs/Web" target="_blank">Web</m3e-breadcrumb-item> <!-- Additional items omitted for brevity --> </m3e-breadcrumb>
Use the slot to render an icon before the label of an item. For icon-only items, simply place an icon in the default slot. Use to provide an accessible label to the inner button of an icon-only item.
When using , the icon in the current breadcrumb item will automatically be filled.
<m3e-breadcrumb> <m3e-breadcrumb-item item-label="Dashboard"> <m3e-icon name="dashboard"></m3e-icon> </m3e-breadcrumb-item> <m3e-breadcrumb-item> <m3e-icon slot="icon" name="analytics"></m3e-icon>Reports </m3e-breadcrumb-item> <m3e-breadcrumb-item> <m3e-icon slot="icon" name="calendar_month"></m3e-icon>Annual </m3e-breadcrumb-item> </m3e-breadcrumb>
Use the slot to provide a custom visual separator. The slotted element is cloned into each item's shadow root, replacing the default separator.
<m3e-breadcrumb> <span slot="separator">/</span> <m3e-breadcrumb-item>Dashboard</m3e-breadcrumb-item> <!-- Additional items omitted for brevity --> </m3e-breadcrumb>
Breadcrumbs do not wrap by default. When horizontal space is limited, the last item is truncated with an ellipsis. Use the attribute if you want the breadcrumb to wrap lines instead.
Quis nostrud exercitation ullamco laboris nisi ut aliquip
<m3e-breadcrumb wrap> <!-- Items omitted for brevity --> </m3e-breadcrumb>
Breadcrumbs are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Breadcrumbs support negative density scales down to -3, with each step reducing the visual height by one density unit. Regardless of visual size, breadcrumb items maintain a 48x48dp minimum touch target to ensure reliable accessibility.
The following illustrates applying density to a breadcrumb.
<m3e-breadcrumb class="density-3"> <!-- Items omitted for brevity --> </m3e-breadcrumb> <!-- Additional breadcrumbs omitted for brevity -->
Breadcrumbs are given ARIA , indicating that the component represents a navigational landmark. Internally, breadcrumbs organize items within a list, with each item exposed using ARIA to identify it as a step in the breadcrumb trail for assistive technologies. Each breadcrumb item encapsulates its slotted content in either an element with or , with the role chosen based on whether an attribute is set.
Separators have to ensure they are always hidden from assistive technologies.
When disabled, a breadcrumb item applies to the internal button or link to convey to assistive technologies that the item is present but not actionable.
When is set, the item becomes non-interactive and exposes only . No internal button or link is rendered, and no interactive role is applied. The component reflects the value of to on the internal wrapper element that contains the slotted content. If no value is explicitly provided, the last breadcrumb item automatically receives to indicate the user's current position. The attribute accepts the same values as the ARIA attribute, including , , , , , and .
For icon-only items, use the attribute to provide an accessible name for the item's internal button or link.
The component arranges multiple into a unified, expressive layout.
Standard button groups add interaction between adjacent buttons
<m3e-button-group> <m3e-icon-button> <m3e-icon name="arrow_back"></m3e-icon> </m3e-icon-button> <m3e-icon-button> <m3e-icon name="arrow_forward"></m3e-icon> </m3e-icon-button> <m3e-icon-button width="wide" variant="filled"> <m3e-icon name="add"></m3e-icon> </m3e-icon-button> <m3e-icon-button> <m3e-icon name="picture_in_picture"></m3e-icon> </m3e-icon-button> <m3e-icon-button> <m3e-icon name="more_vert"></m3e-icon> </m3e-icon-button> </m3e-button-group>
<m3e-button-group> <m3e-button variant="tonal"> <m3e-icon slot="icon" name="play_arrow"></m3e-icon>Start </m3e-button> <m3e-button variant="tonal"> <m3e-icon slot="icon" name="directions_car"></m3e-icon>Directions </m3e-button> <m3e-button variant="tonal"> <m3e-icon slot="icon" name="share"></m3e-icon>Share </m3e-button> </m3e-button-group>
Standard button groups can contain mixed button types and supports narrow and wide icon button widths.
<m3e-button-group> <m3e-icon-button variant="filled" width="wide"> <m3e-icon name="fast_rewind"></m3e-icon> </m3e-icon-button> <m3e-button variant="filled"> <m3e-icon slot="icon" name="play_arrow"></m3e-icon>Play </m3e-button> <m3e-icon-button variant="filled" width="wide"> <m3e-icon name="fast_forward"></m3e-icon> </m3e-icon-button> </m3e-button-group>
Connected button groups behave similarly to standard groups, except they don't affect adjacent buttons and fill available space.
<m3e-button-group variant="connected"> <m3e-button variant="tonal" toggle>Start</m3e-button> <m3e-button variant="tonal" toggle>Directions</m3e-button> <m3e-button variant="tonal" toggle>Share</m3e-button> </m3e-button-group>
Icon buttons will also grow to fill available space.
There are five size variants used to control spacing between button: , (default), , , and . Use the attribute control spacing. To create buttons with a uniform size, set each button's to the same size as the button group.
<m3e-button-group size="large"> <m3e-icon-button size="large"> <m3e-icon name="arrow_back"></m3e-icon> </m3e-icon-button> <m3e-icon-button size="large"> <m3e-icon name="arrow_forward"></m3e-icon> </m3e-icon-button> <m3e-icon-button size="large" width="wide" variant="filled"> <m3e-icon name="add"></m3e-icon> </m3e-icon-button> <m3e-icon-button size="large"> <m3e-icon name="picture_in_picture"></m3e-icon> </m3e-icon-button> <m3e-icon-button size="large"> <m3e-icon name="more_vert"></m3e-icon> </m3e-icon-button> </m3e-button-group>
Connected button groups always have the same spacing between buttons, regardless of size.
<m3e-button-group size="large" variant="connected"> <m3e-button variant="tonal" size="large" toggle>Start</m3e-button> <m3e-button variant="tonal" size="large" toggle>Directions</m3e-button> <m3e-button variant="tonal" size="large" toggle>Share</m3e-button> </m3e-button-group>
Button groups containing toggle buttons support both single-select and multi-select modes. Use the attribute to allow multiple toggle buttons to be selected simultaneously.
<m3e-button-group multi> <m3e-icon-button variant="tonal" toggle> <m3e-icon name="format_bold"></m3e-icon> </m3e-icon-button> <m3e-icon-button variant="tonal" toggle> <m3e-icon name="format_italic"></m3e-icon> </m3e-icon-button> <m3e-icon-button variant="tonal" toggle> <m3e-icon name="format_underlined"></m3e-icon> </m3e-icon-button> </m3e-button-group>
Buttons and icon buttons in a group are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Buttons support negative density scales down to -3, with each step reducing the visual height by one density unit. Regardless of visual size, buttons maintain a 48x48dp minimum touch target to ensure reliable accessibility.
<m3e-button-group class="density-3"> <!-- Buttons omitted for brevity --> </m3e-button-group> <!-- Additional groups and variants omitted for brevity -->
By default, button groups are given the ARIA role , indicating a set of related user interface elements that are grouped together. For a single-select group containing one or more toggle buttons, button groups are given the ARIA role and buttons are given the role .
Always provide an accessible label via or for each button group.
You also need a module script for and due to being a dependency.
The component is a semantic, expressive UI primitive users interact with to perform an action.
<m3e-button variant="elevated">Elevated</m3e-button> <!-- Additional variants omitted for brevity -->
Buttons can come in two shapes: (default) or . Use the attribute to control the shape of a button.
<m3e-button variant="elevated" shape="square">Square Elevated</m3e-button> <!-- Additional variants omitted for brevity -->
There are five size variants which can be used to add or remove emphasis: , (default), , , and . Use the attribute to control a button's size.
<m3e-button variant="tonal" size="extra-small">Extra Small</m3e-button> <!-- Additional sizes omitted for brevity -->
Use the slot to render an icon before the button's label. In addition, use the slot to render an icon after the button's label. While not required, the following example uses the component from to present icons using Material Symbols.
<m3e-button variant="tonal"> <m3e-icon slot="icon" name="send"></m3e-icon>Send </m3e-button> <m3e-button variant="tonal"> Open<m3e-icon slot="trailing-icon" name="open_in_new_window"></m3e-icon> </m3e-button>
Use the attribute to control whether a button can be used for binary selection and the attribute to control the selected state. When clicked, a cancellable event is emitted followed by an and event. To cancel selection change, call during .
<m3e-button variant="elevated" toggle>Elevated Toggle</m3e-button> <!-- Additional variants omitted for brevity -->
In addition, you can use the and slots to specify an alternate label and icon when selected. When using the to present icons, the icon will automatically be filled when a button is selected.
<m3e-button variant="filled" toggle> <m3e-icon slot="icon" name="play_arrow"></m3e-icon> <m3e-icon slot="selected-icon" name="stop"></m3e-icon> Start <span slot="selected">Stop</span> </m3e-button>
Use the attribute to control the behavior of a button. By default, is . You can change this to or to submit or reset a parenting form when clicked. When submitting a form, you can use the and attributes to specify a value.
Buttons can also behave like links by specifying a URL using the attribute. You can also specify , and similar to native anchor tags.
<m3e-button variant="tonal" href="https://www.google.com" target="_blank"> Google<m3e-icon slot="trailing-icon" name="open_in_new_window"></m3e-icon> </m3e-button>
Buttons are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Buttons support negative density scales down to -3, with each step reducing the visual height by one density unit. Regardless of visual size, buttons maintain a 48x48dp minimum touch target to ensure reliable accessibility.
The following illustrates applying density to the and sized buttons.
<m3e-button variant="filled" size="extra-small" class="density-3"> <m3e-icon slot="icon" name="stars"></m3e-icon> Density -3 </m3e-button> <!-- Additional buttons omitted for brevity -->
By default, buttons are given the ARIA role , indicating they are clickable elements that trigger actions.
When disabling using either the or attribute, is used to convey to assistive technologies that the button is disabled. If disabled using , is retained to convey that the button continues to be interactive.
When is specified, the ARIA role is used instead of to convey the button provides an interactive reference to a resource. In addition, the component presents an internal anchor, hidden from assistive technologies, used to invoke native browser features such a context menu options and tooltips.
For toggle buttons, is used to convey the selected state, even when disabled.
The component provides structured navigation and selection across month, year, and multi-year views. It supports single-date and range selection, applies disabled rules including minimum, maximum, and blackout constraints, and provides styling hooks for special date states.
Set the attribute to define the currently selected date. Listen for the event to respond when the user selects a new one.
To select a range of dates instead of a single date, use the and attributes. Setting puts the calendar into range-selection mode. When the user selects a date after the current , both and are updated to that value. If the user selects a date before the current , the range is reset and both and are set to the newly selected date.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js" "@m3e/web/core/anchoring": "/node_modules/@m3e/web/dist/core-anchoring.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js", "@m3e/web/core/platform": "/node_modules/@m3e/web/dist/core-platform.js" "@m3e/web/button": "/node_modules/@m3e/web/dist/button.js", "@m3e/web/icon-button": "/node_modules/@m3e/web/dist/icon-button.js", "@m3e/web/tooltip": "/node_modules/@m3e/web/dist/tooltip.js" } } </script>
The component is a flexible, expressive container for presenting a unified subject—text, media, and actions—on a visually distinct surface.
— Renders the header region of the card. Typically used for titles, icons, or introductory context.
— Renders the padded content region. Ideal for body text, form fields, or structured information.
— Renders the action region. Use for buttons, toggles, or interactive controls aligned to the card's intent.
— Renders the footer region. Suitable for metadata, disclaimers, or secondary actions.
— Renders the default content of the card without padding. Use for full-bleed layouts or custom compositions.
There are three card appearance variants: (default), , and . Use the attribute to control a card's appearance.
<m3e-card variant="filled"> <div slot="content">Filled</div> </m3e-card> <!-- Additional variants omitted for brevity -->
By default, cards are rendered as block-level elements. Use the attribute to make a card render inline with surrounding content.
By default, cards are oriented vertically. Use the attribute to change the orientation to or (default).
<m3e-card class="image-card" inline orientation="horizontal"> <img slot="header" src="[YOUR IMAGE URL HERE]" /> <div slot="content">Horizontal</div> </m3e-card>
Use the attribute to configure a card as an actionable surface, allowing users to interact with the entire card as a single affordance. This pattern is ideal for navigation, selection, or triggering contextual behavior.
<m3e-card variant="filled" actionable> <div slot="content">Filled</div> </m3e-card> <!-- Additional variants omitted for brevity -->
<m3e-card variant="filled" actionable disabled> <div slot="content">Disabled</div> </m3e-card> <br /> <m3e-card variant="filled" actionable disabled-interactive> <div slot="content">Disabled Interactive</div> </m3e-card>
Actionable cards can also behave like links by specifying a URL using the attribute. You can also specify , and similar to native anchor tags.
By default, cards are not given an ARIA role, indicating that they are treated as neutral, non-semantic elements in the accessibility tree unless explicitly referenced or annotated.
Actionable cards are given ARIA indicating they are clickable elements that trigger actions.
When disabling using either the or attribute, is used to convey to assistive technologies that the actionable card is disabled. If disabled using , is retained to convey that the card continues to be interactive.
When is specified, the ARIA role is used instead of to convey the actionable card provides an interactive reference to a resource. In addition, the component presents an internal anchor, hidden from assistive technologies, used to invoke native browser features such a context menu options and tooltips.
The component enables users to select one or more options from a set.
Checkboxes may be standalone, pre-checked, or indeterminate. When clicked, a cancellable event is emitted followed by an and event. To cancel checked state changes, call during .
Associate a label with a checkbox using the element.
<label><m3e-checkbox></m3e-checkbox> Checkbox 1</label> <m3e-checkbox id="chk2"></m3e-checkbox> <label for="chk2">Checkbox 2</label>
<label><m3e-checkbox disabled></m3e-checkbox>Disabled Checkbox 1</label> <m3e-checkbox id="chk3" disabled></m3e-checkbox> <label for="chk3">Disabled Checkbox 2</label>
Use the attribute to control whether a checkbox must be checked. The event fires when a form is submitted and the element fails constraint validation. When invalid, labels are automatically styled accordingly.
<form> <label> <m3e-checkbox name="accepted" required></m3e-checkbox>I accept the Terms and Conditions </label> <br /> <m3e-button variant="filled" type="submit">Submit</m3e-button> </form>
Checkboxes are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Checkboxes support negative density scales down to -3, with each step reducing the visual height by one density unit. Regardless of visual size, checkboxes maintain a 48x48dp minimum touch target to ensure reliable accessibility.
The following illustrates applying density to a checkbox. Interact with each checkbox to see how density impacts its state layer.
<label> <m3e-checkbox class="density-3"></m3e-checkbox> Density -3 </label> <!-- Additional checkboxes omitted for brevity -->
Checkboxes are given the ARIA aligning its semantic identity with native checkbox controls. This ensures screen readers and assistive technologies interpret its behavior correctly.
The component manages and reflects its state via :
When disabled using the attribute, is used to convey to assistive technologies that the checkbox is disabled. When disabled at runtime, it does
The module provides expressive, accessible chip components for actions, input, filtering, and suggestions.
Use the component to present a non-interactive chip. You can optionally nest chips in a which provides default spacing between chips.
All chip types support two appearance variants: (default) and . Use the attribute to change a chip's appearance.
Chips can be stacked vertically using the attribute of a .
The provides slots for two icons. Use the slot to present an icon before a chip's label and use the slot to present an icon after it. While not required, the following example uses the component from to present icons using Material Symbols.
Use the to perform a smart or automated action that can span multiple applications.
Use the attribute to control the behavior of an assist chip. By default, is . You can change this to or to submit or reset a parenting form when clicked. When submitting a form, you can use the and attributes to specify a value.
Assist chips can also behave like links by specifying a URL using the attribute. You can also specify , and similar to native anchor tags.
Use the and components to present chips that users can select or deselect to filter content or data sets.
The provides slots for two icons. Use the slot to present an icon before a chip's label and use the slot to present an icon after it.
When selected, a filter chip's icon is replaced with a check icon.
<m3e-filter-chip-set aria-label="Filter by topic"> <m3e-filter-chip selected> <m3e-icon slot="icon" name="palette"></m3e-icon>Design </m3e-filter-chip> <!-- Additional chips omitted for brevity --> </m3e-filter-chip-set>
Filter chips support both single-select and multi-select modes. Use the attribute of to allow multiple chips to be selected simultaneously.
<m3e-filter-chip-set aria-label="Filter by topic" multi> <m3e-filter-chip selected> <m3e-icon slot="icon" name="palette"></m3e-icon>Design </m3e-filter-chip> <m3e-filter-chip selected> <m3e-icon slot="icon" name="accessibility_new"></m3e-icon>Accessibility </m3e-filter-chip> <!-- Additional chips omitted for brevity --> </m3e-filter-chip-set>
<m3e-filter-chip-set aria-label="Filter by topic"> <m3e-filter-chip disabled> <m3e-icon slot="icon" name="palette"></m3e-icon>Design </m3e-filter-chip> <m3e-filter-chip disabled-interactive> <m3e-icon slot="icon" name="accessibility_new"></m3e-icon>Accessibility </m3e-filter-chip> <!-- Additional chips omitted for brevity --> </m3e-filter-chip-set>
You can also use the attribute of to disable the chip set in its entirety.
<m3e-filter-chip-set aria-label="Filter by topic" multi disabled> <!-- Chips omitted for brevity --> </m3e-filter-chip-set>
Use the attribute of to hide selection indicators. If a filter chip has an icon via use of the slot, the icon is retained upon selection.
<m3e-filter-chip-set aria-label="Filter by topic" hide-selection-indicator> <m3e-filter-chip>Design </m3e-filter-chip> <m3e-filter-chip>Accessibility </m3e-filter-chip> <m3e-filter-chip><m3e-icon slot="icon" name="motion_photos_on"></m3e-icon>Motion </m3e-filter-chip> <m3e-filter-chip><m3e-icon slot="icon" name="description"></m3e-icon>Documentation </m3e-filter-chip> </m3e-filter-chip-set>>
Filter chips can participate in form submission as a
(FACE), contributing its state to the form data. The attribute defines the key under which the component's value is submitted in a form. The chip set's value is determined by selected chips. You can use the of a to control a chip's value. Otherwise, the value is derived from the chip's label (default slot). When multiple chips are selected, form data will include multiple entries keyed by the same .
The component enables users to input, display, and manage a collection of discrete values as input chips. Designed for expressive, accessible forms, it supports keyboard navigation, validation, and seamless integration with . See
Use the slot of the to designate the input element associated with the component. When is fired on the input, the chip set adds a new removable to the set. Users can press to remove chips or click the remove button on individual chips. If a chip is focused, users can press to remove the chip.
The component raises a event when chips are added to, or removed from, the set. Use the event's to determine whether the type of change was or . The event's also includes and properties.
<m3e-form-field> <label slot="label" for="keywords">Keywords</label> <m3e-input-chip-set aria-label="Enter keywords"> <input id="keywords" slot="input" placeholder="New keyword..." /> </m3e-input-chip-set> </m3e-form-field>
You can prepopulate a with chips using the component. Use the attribute to control whether a button is presented that allows the user to remove an input chip. This also controls whether can be used to delete a chip using the keyboard.
<m3e-form-field> <label slot="label" for="keywords">Keywords</label> <m3e-input-chip-set aria-label="Enter keywords"> <m3e-input-chip removable>Design</m3e-input-chip> <m3e-input-chip removable>Accessibility</m3e-input-chip> <m3e-input-chip removable>Motion</m3e-input-chip> <input id="keywords" slot="input" placeholder="New keyword..." /> </m3e-input-chip-set> </m3e-form-field>
Use the attribute to control how many chips are shown when the input is not focused. When the number of chips exceeds this value, the component collapses the chip set and shows a summary indicator (for example, ) instead of rendering all chips.
When the input is focused, all chips are displayed regardless of the setting.
<m3e-form-field> <label slot="label" for="keywords">Keywords</label> <m3e-input-chip-set aria-label="Enter keywords" max-chips="1"> <m3e-input-chip removable>Design</m3e-input-chip> <m3e-input-chip removable>Accessibility</m3e-input-chip> <m3e-input-chip removable>Motion</m3e-input-chip> <input id="keywords" slot="input" placeholder="New keyword..." /> </m3e-input-chip-set> </m3e-form-field>
Use the attribute of to disable the chip set in its entirety, including its corresponding and any prepopulated chips. In addition, chips cannot be removed when disabled.
<m3e-form-field> <label slot="label" for="keywords">Keywords</label> <m3e-input-chip-set aria-label="Enter keywords" disabled> <m3e-input-chip removable>Design</m3e-input-chip> <m3e-input-chip removable>Accessibility</m3e-input-chip> <m3e-input-chip removable>Motion</m3e-input-chip> <input id="keywords" slot="input" placeholder="New keyword..." /> </m3e-input-chip-set> </m3e-form-field>
Input chips can participate in form submission as a
(FACE), contributing its state to the form data. The attribute defines the key under which the component's value is submitted in a form. The chip set's value is determined by its chips. When multiple chips are specified, form data will include multiple entries keyed by the same .
Use the component to offer users contextually relevant actions or responses. This component is visually and functionally equivalent to , differing only in semantic intent. It encodes contextual suggestion rather than passive assistance, guiding user action through implied relevance.
<m3e-suggestion-chip> <m3e-icon slot="icon" name="lightbulb"></m3e-icon> Try budget planner </m3e-suggestion-chip>
Use the attribute to control the behavior of a suggestion chip. By default, is . You can change this to or to submit or reset a parenting form when clicked. When submitting a form, you can use the and attributes to specify a value.
Suggestion chips can also behave like links by specifying a URL using the attribute. You can also specify , and similar to native anchor tags.
Chips are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Chips support negative density scales down to -3, with each step reducing the visual height by one density unit. Regardless of visual size, chips maintain a 48x48dp minimum touch target to ensure reliable accessibility.
The following illustrates applying density to assist chips.
<m3e-assist-chip class="density-3"> <m3e-icon slot="icon" name="stars"></m3e-icon> Density -3 </m3e-assist-chip> <!-- Additional chips omitted for brevity -->
By default, and are not given an ARIA role, indicating that they are treated as neutral, non-semantic elements in the accessibility tree unless explicitly referenced or annotated.
Chip sets, regardless of variant (e.g., filter, input, suggestion), are annotated with when rendered in a vertical layout. This attribute communicates directional flow to assistive technologies, ensuring spatial clarity and navigational predictability for screen reader users.
By default, action and suggestion chips are given the ARIA role , indicating they are clickable elements that trigger actions. When is specified, the ARIA role is used instead of to convey the chip provides an interactive reference to a resource. In addition, the chip presents an internal anchor, hidden from assistive technologies, used to invoke native browser features such a context menu options and tooltips.
When disabling using either the or attribute, is used to convey to assistive technologies that the chip is disabled. If disabled using , is retained to convey that the chip continues to be interactive.
Filter chip sets are given the ARIA and chips are given for single-select chip sets. For multi-select, sets are given and chips are given . When selected, the chip's selection state is reflected using for single-select chips sets and for multi-select chips sets.
Input chip sets are given ARIA and chips are given . Additionally, the internal content of an input chip (icons and label) are given ARIA and the remove button's container is given . This pattern enables assistive technologies to interpret input chips as structured, interactive rows—enhancing clarity, predictability, and discoverability for users navigating via screen readers or keyboard.
The component renders a shaped surface with padding and vertical scrolling for document-like content.
Use the to render a shaped surface with padding and vertical scrolling for document-like content. Inline padding is automatically adjusted to provide uniform spacing by compensating for platform scrollbar width, ensuring the content remains visually centered.
The following example is shown inside a filled card to demonstrate the content pane's default surface color and shape.
Lorem ipsum dolor sit amet, consectetur adipiscing elit. Sed vitae ligula at ipsum pulvinar tincidunt. Integer feugiat, tortor non aliquet facilisis, velit risus faucibus lorem, vitae porttitor justo arcu nec sapien. Curabitur euismod, urna vel placerat dictum, augue sem ullamcorper velit, id interdum neque magna non nisl. Vestibulum ante ipsum primis in faucibus orci luctus et ultrices posuere cubilia curae. Suspendisse potenti. Praesent ac orci eget urna volutpat fermentum. Donec non mi sed sapien gravida aliquet. Vivamus at orci id libero scelerisque convallis. Pellentesque habitant morbi tristique senectus et netus et malesuada fames ac turpis egestas. Cras ac erat id velit pharetra luctus. Mauris sed nisl sed arcu facilisis tincidunt. Aliquam erat volutpat. Sed sit amet massa non magna gravida cursus. Sed vulputate, velit id suscipit convallis, lorem ipsum varius neque, sed porttitor lacus justo vitae libero. Integer at felis vel lacus porta posuere. Aenean non lorem ac nulla gravida tincidunt. Pellentesque vel urna id libero dictum gravida. Donec sit amet velit nec sapien ultricies tincidunt. Vivamus in augue id libero sodales tincidunt. Integer id lorem nec sapien bibendum tincidunt. Sed id lacus non justo viverra tincidunt. Curabitur id risus vitae justo tincidunt gravida. Vivamus id ligula non ipsum porta tincidunt. Pellentesque id lorem nec sapien dictum tincidunt. Integer id lorem nec sapien bibendum tincidunt.
<m3e-content-pane> <m3e-heading variant="display" size="large">Content header</m3e-heading> <p> <!-- Content omitted for brevity --> </p> </m3e-content-pane>
By default, content panes are not given an ARIA role, indicating that they are treated as neutral, non-semantic elements in the accessibility tree unless explicitly referenced or annotated.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js" } } </script>
The component provides a segmented input for editing date and/or time values. It supports date-only, time-only, and combined date/time entry, and integrates with , and . Each segment is independently editable for precise, accessible, form-friendly interaction. See
The is intended to be used inside . The example below shows that usage. See
<m3e-form-field variant="outlined"> <label slot="label" for="field">Date Field</label> <m3e-date-input id="field"></m3e-date-input> <span slot="hint">MM/DD/YYYY</span> </m3e-form-field>
Use the attribute to set or read the date. The and fire only when all segments are fully populated. A fires when focus leaves the control.
<m3e-form-field variant="outlined"> <label slot="label" for="field">Date Field</label> <m3e-date-input id="field" value="2026-01-01"></m3e-date-input> <span slot="hint">MM/DD/YYYY</span> </m3e-form-field>
(FACE), contributing its state to the form data. The attribute defines the key under which the component's value is submitted in a form. When not disabled, is submitted as a string value in ISO format.
Use the attribute to control whether edits (default), , or values.
When editing -only, the existing date portion of the value is preserved. When editing -only, the existing time portion is preserved.
When creating a new in mode, the time defaults to midnight. When creating a new in mode, the date defaults to the current day.
The following examples illustrate editing time and date/time values.
<m3e-form-field variant="outlined"> <label slot="label" for="field1">Time Field</label> <m3e-date-input id="field1" type="time" value="2026-01-01T23:30:00Z"></m3e-date-input> <span slot="hint">HH:MM</span> </m3e-form-field> <m3e-form-field variant="outlined"> <label slot="label" for="field2">Date-time Field</label> <m3e-date-input id="field2" type="datetime" value="2026-01-01T23:30:00Z"></m3e-date-input> <span slot="hint">MM/DD/YYYY HH:MM</span> </m3e-form-field>
The and integrate with using the attribute, which binds the picker to the input by referencing the input's ID. See
When a picker is bound to , the input's own configuration takes precedence. Properties such as , , , , , , , and override the corresponding settings on the picker. This ensures the input and picker remain consistent and enforce the same constraints.
When is used, a single may link to both a and a for independent date and time selection.
<m3e-form-field variant="outlined"> <label slot="label" for="field1">Date Picker</label> <m3e-date-input id="field1"></m3e-date-input> <m3e-icon-button aria-label="Open calendar" slot="suffix"> <m3e-icon name="calendar_today"></m3e-icon> <m3e-datepicker-toggle for="datepicker1"></m3e-datepicker-toggle> </m3e-icon-button> <span slot="hint">MM/DD/YYYY</span> </m3e-form-field> <m3e-datepicker id="datepicker1" for="field1"></m3e-datepicker> <m3e-form-field variant="outlined"> <label slot="label" for="field2">Time Picker</label> <m3e-date-input id="field2" type="time"></m3e-date-input> <m3e-icon-button aria-label="Open time picker" slot="suffix"> <m3e-icon name="schedule"></m3e-icon> <m3e-timepicker-toggle for="timepicker1"></m3e-timepicker-toggle> </m3e-icon-button> <span slot="hint">HH:MM</span> </m3e-form-field> <m3e-timepicker id="timepicker1" for="field2"></m3e-timepicker> <m3e-form-field variant="outlined"> <label slot="label" for="field3">Datetime Picker</label> <m3e-date-input id="field3" type="datetime"></m3e-date-input> <m3e-icon-button aria-label="Open calendar" slot="suffix"> <m3e-icon name="calendar_today"></m3e-icon> <m3e-datepicker-toggle for="datepicker2"></m3e-datepicker-toggle> </m3e-icon-button> <m3e-icon-button aria-label="Open time picker" slot="suffix"> <m3e-icon name="schedule"></m3e-icon> <m3e-timepicker-toggle for="timepicker2"></m3e-timepicker-toggle> </m3e-icon-button> <span slot="hint">MM/DD/YYYY HH:MM</span> </m3e-form-field> <m3e-datepicker id="datepicker2" for="field3"></m3e-datepicker> <m3e-timepicker id="timepicker2" for="field3"></m3e-timepicker>
The attribute controls the hour cycle used by the input. It accepts (default), , or .
— forces a 12-hour clock with an AM/PM meridiem.
— forces a 24-hour clock without a meridiem.
— selects 12-hour or 24-hour based on the user's locale.
<m3e-form-field variant="outlined"> <label slot="label" for="field1">Time Field</label> <m3e-date-input id="field1" type="time" time-format="24" value="2026-01-01T23:30:00Z"> </m3e-date-input> <span slot="hint">HH:MM</span> </m3e-form-field> <m3e-form-field variant="outlined"> <label slot="label" for="field2">Date-time Field</label> <m3e-date-input id="field2" type="datetime" time-format="24" value="2026-01-01T23:30:00Z"> </m3e-date-input> <span slot="hint">MM/DD/YYYY HH:MM</span> </m3e-form-field>
Use the attribute to display the seconds segment when editing time.
<m3e-form-field variant="outlined"> <label slot="label" for="field1">Time Field</label> <m3e-date-input id="field1" type="time" show-seconds value="2026-01-01T23:30:00Z"> </m3e-date-input> <span slot="hint">HH:MM:SS</span> </m3e-form-field> <m3e-form-field variant="outlined"> <label slot="label" for="field2">Date-time Field</label> <m3e-date-input id="field2" type="datetime" show-seconds value="2026-01-01T23:30:00Z"> </m3e-date-input> <span slot="hint">MM/DD/YYYY HH:MM:SS</span> </m3e-form-field>
<m3e-form-field variant="outlined"> <label slot="label" for="field">Date Field</label> <m3e-date-input id="field" value="2026-01-01" disabled></m3e-date-input> <span slot="hint">MM/DD/YYYY</span> </m3e-form-field>
Use the attribute to prevent editing while still allowing focus and segment navigation.
Similar to disabled, when associated with a picker, the picker's toggle button will automatically be disabled when the input is read-only.
<m3e-form-field variant="outlined"> <label slot="label" for="field">Date Field</label> <m3e-date-input id="field" value="2026-01-01" readonly></m3e-date-input> <span slot="hint">MM/DD/YYYY</span> </m3e-form-field>
Apply the attribute to mark the input as a mandatory field.
<m3e-form-field variant="outlined"> <label slot="label" for="field">Date Field</label> <m3e-date-input id="field" required></m3e-date-input> <span slot="hint">MM/DD/YYYY</span> </m3e-form-field>
Use the and attributes to mark dates before or after the respective values as invalid.
<m3e-form-field variant="outlined"> <label slot="label" for="field">Date Field</label> <m3e-date-input id="field" value="2026-01-01" min-date="2026-01-01" max-date="2026-04-30"> </m3e-date-input> <span slot="hint">MM/DD/YYYY</span> </m3e-form-field>
In addition to and constraints, you can use the property to specify a function used to determine whether a date invalid.
Use the and attributes to mark times before or after the respective values as invalid.
<m3e-form-field variant="outlined"> <label slot="label" for="field">Time Field</label> <m3e-date-input id="field" type="time" value="2026-01-01T13:15:00Z" min-time="8:15 AM" max-time="5:30 PM" ></m3e-date-input> <span slot="hint">HH:MM</span> </m3e-form-field>
In addition to and constraints, the property lets you provide a function that determines whether a given time is invalid. The function receives a object containing the , , and values for the time being evaluated.
document.querySelector("#blackout-times").blackoutTimes = (t) => t.hour < 8 || t.hour > 18;
Use the property to override specific validation messages. Unspecified messages continue to use the component's defaults.
— No value is provided when one is required.
— The segments of the input are partially populated.
— The value is earlier than and/or .
— The value is later than and/or .
— The value falls within a disabled (blackout) range.
These flags are combined to determine the final validation state and the message shown to the user.
The date and time format is automatically determined by the user's locale and cannot be customized.
Segment order follows , ensuring the correct reading order for all locales. Segment labels ( , , ) are provided by the component and remain consistent across locales.
The component exposes each date and time segment as an individual for assistive technologies. Screen readers announce the segment label (month, day, year, hour, minute, second), the current value, and the allowed range, ensuring clear feedback as the user edits each part of the value.
Keyboard interaction follows native segmented input patterns. Arrow keys increment or decrement the focused segment, digit keys replace the segment value, and left/right arrows move between segments. Focus remains within the component. Clearing a segment does not move focus, and additional delete/backspace presses navigate to the previous segment.
Validation states map to standard HTML constraint validation. Empty required fields report , and partially entered values report . Range constraints use and for values outside the configured , , , and . Blackout dates and times are reported as . All validity states integrate with error messaging.
The component supports accessible naming by linking to an external label through or a element using , ensuring the field is correctly announced by assistive technologies.
The component presents a temporary, structured surface for selecting dates across month, year, and multi-year views. It supports single-date and range selection, enforces minimum, maximum, and blackout constraints, and exposes styling hooks for highlighting special date states.
Use the attribute to set the currently selected date, and listen for the event to react when the user selects a new one. You can also enable the attribute to display a button that clears the current selection.
<!-- Form field omitted for brevity --> <m3e-datepicker id="datepicker" date="2026-01-01" clearable></m3e-datepicker>
const picker = document.querySelector("#datepicker"); const field = document.querySelector("#field"); field.value = toLocaleDateString(picker.date); picker.addEventListener("change", () => { field.value = toLocaleDateString(picker.date); });
By default, a datepicker opens to the selected date, or to today if no date is set. You can override this by using the attribute to specify the date that should initially display.
<!-- Form field omitted for brevity --> <m3e-datepicker id="datepicker" start-at="2026-01-01"></m3e-datepicker>
<!-- Form field omitted for brevity --> <m3e-datepicker id="datepicker" start-view="multi-year"></m3e-datepicker>
To select a range of dates instead of a single date, use the , or and attributes. Setting or puts the calendar into range-selection mode.
<m3e-form-field variant="outlined"> <label slot="label" for="field">Date Range Field</label> <input autocomplete="off" id="field" /> <m3e-icon-button slot="suffix"> <m3e-icon name="calendar_today"></m3e-icon> <m3e-datepicker-toggle for="range-picker"></m3e-datepicker-toggle> </m3e-icon-button> <span slot="hint">MM/DD/YYYY - MM/DD/YYYY</span> </m3e-form-field> <m3e-datepicker id="range-picker" range-start="2026-01-01" range-end="2026-01-09" start-at="2026-01-01" ></m3e-datepicker>
const picker = document.querySelector("#range-picker"); const field = document.querySelector("#field"); field.value = toLocaleDateString(picker.rangeStart) + " - " + toLocaleDateString(picker.rangeEnd); picker.addEventListener("change", () => { field.value = toLocaleDateString(picker.rangeStart) + " - " + toLocaleDateString(picker.rangeEnd); });
Use the and to disable all dates on the calendar before or after the respective values and prevent the user from advancing the calendar past the month or year (depending on current view) containing the minimum or maximum date.
<!-- Form field omitted for brevity --> <m3e-datepicker id="picker1" start-at="2026-04-01" min-date="2026-01-01" max-date="2026-04-30" ></m3e-datepicker>
In addition to and constraints, you can use the property to specify a function used to determine whether a date is disabled and cannot be selected by the user.
Datepickers are given ARIA to identify the picker surface as a discrete interactive region that opens above the page and requires user attention. When the picker is shown in its modal variant, it also sets to indicate that the rest of the page is inert and that focus is trapped within the dialog until it is dismissed. In docked mode, the picker omits entirely, since the surrounding page remains interactive and the surface behaves like an anchored popover rather than a blocking dialog.
Datepicker toggles add to its parenting element indicating to assistive technologies that activating the control will open a dialog.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js" "@m3e/web/core/anchoring": "/node_modules/@m3e/web/dist/core-anchoring.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js", "@m3e/web/core/platform": "/node_modules/@m3e/web/dist/core-platform.js" "@m3e/web/calendar": "/node_modules/@m3e/web/dist/calendar.js", "@m3e/web/button": "/node_modules/@m3e/web/dist/button.js", "@m3e/web/icon-button": "/node_modules/@m3e/web/dist/icon-button.js", "@m3e/web/tooltip": "/node_modules/@m3e/web/dist/tooltip.js" } } </script>
The component presents important prompts, alerts, and actions in user flows.
The dialog scrim will only overlay the current window.
The dialog is positioned relative the current window.
The component supports structured composition through named slots, enabling flexible layout and expressive content delivery:
— Renders the main content of the dialog.
— Renders the header of the dialog. Typically used for titles, summaries, or introductory context.
— Renders the actions of the dialog. Commonly includes buttons such as "OK", "Cancel", or custom actions.
— Renders the icon of the button used to dismiss the dialog.
Lorem ipsum dolor sit amet, consectetur adipiscing elit, sed do eiusmod tempor incididunt ut labore et dolore magna aliqua. Ut enim ad minim veniam, quis nostrud exercitation ullamco laboris nisi ut aliquip ex ea commodo consequat. Duis aute irure dolor in reprehenderit in voluptate velit esse cillum dolore eu fugiat nulla pariatur. Excepteur sint occaecat cupidatat non proident, sunt in culpa qui officia deserunt mollit anim id est laborum.
<m3e-button variant="filled"> <m3e-dialog-trigger for="dlg">Open Dialog</m3e-dialog-trigger> </m3e-button> <m3e-dialog id="dlg" dismissible> <span slot="header">Lorem ipsum dolor sit amet</span> <!-- Content omitted for brevity --> <div slot="actions" end> <m3e-button autofocus><m3e-dialog-action return-value="ok">Close</m3e-dialog-action></m3e-button> </div> </m3e-dialog>
Use the to open a dialog. This component should be nested inside a clickable element and be associated with a dialog by setting the attribute to the of the dialog to open.
When its parent is clicked, it opens the dialog. When the dialog is closed, focus is stored to the parenting element.
Alternatively, you can programmatically open a dialog using the method.
<m3e-button variant="filled"> <m3e-dialog-trigger for="dlg">Open Dialog</m3e-dialog-trigger> </m3e-button> <m3e-dialog id="dlg" dismissible> <!-- Content omitted for brevity --> </m3e-dialog>
When closed, the emits a event. Actions are used to designate a return value, set on the dialog when closed. Use the component define an action.
When the parenting element of a is clicked, it sets the property of its parenting to the value of the attribute and closes the dialog.
Alternatively, you can programmatically close a dialog using the method.
By default, a close (or dismiss) button is not presented for a dialog. Use the attribute of the to control this behavior. You can change the icon using the slot and change the accessible label using the attribute.
The component provides an accessible experience by allowing users to click a dialog's backdrop or press to close a dialog. When this occurs, the event is emitted. Use the attribute of the to disable this behavior.
The component provides an accessible experience by providing a focus trap used to keep focus within a dialog. Use the attribute of the to disable this behavior.
In addition, you can use the attribute to specify which action should be focused when the dialog is opened.
The presents an internal element labelled by the content of the slot. You can use the attribute to specify whether the ARIA is applied to the internal dialog.
Dialog triggers add to its parenting element indicating to assistive technologies that activating the control will open a dialog.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js", "@m3e/web/icon-button": "/node_modules/@m3e/web/dist/icon-button.js" } } </script>
The component visually separates content within layouts, lists, or containers using a thin, unobtrusive line.
A can be used on its own to create a horizontal (or vertical) line.
Use the attribute to control whether the divider is inset. Alternatively, use and to control the start or end inset.
Use the attribute to control whether a divider is oriented vertically.
<m3e-divider vertical></m3e-divider> <m3e-divider vertical inset></m3e-divider> <m3e-divider vertical inset-start></m3e-divider> <m3e-divider vertical inset-end></m3e-divider>
The applies the ARIA , implementing a non-focusable separator used to separate content.
The is a responsive layout container that manages collapsible left and right drawers alongside main content.
The provides the and slots for specifying drawer elements. The default slot presents the content between drawers. The and attributes control whether the corresponding drawer is open.
<m3e-drawer-container start end> <div slot="start">Start drawer</div> <div>Main content</div> <div slot="end">End drawer</div> </m3e-drawer-container>
You can control the drawer widths using the CSS custom property or by explicitly setting the width of a drawer.
<m3e-drawer-container start> <div slot="start" style="width: 200px;">Start drawer</div> <div>Main content</div> </m3e-drawer-container>
: Mode adapts responsively based on viewport size.
The and modes present a scrim overlaying main content, a focus trap keeps focus in drawers, and main content becomes inert. Clicking the scrim will automatically close drawers.
Use the attribute to control the behavior of the start drawer, and to configure the end drawer independently.
<m3e-drawer-container start start-mode="push"> <div slot="start">Start drawer</div> <div>Main content</div> </m3e-drawer-container>
<m3e-drawer-container start start-mode="over"> <div slot="start">Start drawer</div> <div>Main content</div> </m3e-drawer-container>
Use the or to separate the drawer from the content using a divider when . When the and resolves to , the dividers are shown as well.
<m3e-drawer-container end end-mode="side" end-divider> <div>Main content</div> <div slot="end">End drawer</div> </m3e-drawer-container>
Use the to toggle the opened state of a drawer. This component should be nested inside a clickable element and be associated with a slotted drawer by setting the attribute to the of a drawer.
<m3e-icon-button slot="leading-icon" aria-label="Menu" toggle> <m3e-icon name="menu"></m3e-icon> <m3e-icon slot="selected" name="menu_open"></m3e-icon> <m3e-drawer-toggle for="nav-drawer"></m3e-drawer-toggle> </m3e-icon-button> <m3e-drawer-container start-mode="over"> <div slot="start" id="nav-drawer">Start drawer</div> <div>Main content</div> </m3e-drawer-container>
The is not given an ARIA role, indicating that it is treated as a neutral, non-semantic element in the accessibility tree.
Drawers should each be given an appropriate ARIA role. Consider using a element or ARIA for start drawers that present links used to navigate an application. An end drawer may use a element or be given the ARIA when its content is related to the main content, but not essential to understanding it. Otherwise, is a good fallback.
Consider nesting a element inside a 's default slot when its used as the primary drawer container for an application. Otherwise, consider using a single with ARIA .
Drawer toggles use to associate its parenting element with the drawer, and dynamically update to indicate whether the drawer is open.
The module provides components for organizing content in collapsible sections and coordinated groups. It includes the following components:
— A details-summary view for organizing content in collapsible sections.
— Organizes multiple expansion panels into a coordinated group.
You can use a standalone to present a collapsible details-summary section. Use the slot to specify the title of a panel and use the default slot for its content.
Use the attribute to control whether a panel is open (expanded).
<m3e-expansion-panel open> <span slot="header">Panel header</span> Panel contents </m3e-expansion-panel>
You can change the toggle position and direction using the and attributes. Supported positions include: and (default). Supported directions include (default) and .
<m3e-expansion-panel toggle-position="before" toggle-direction="horizontal"> <span slot="header">Panel header</span> Panel contents </m3e-expansion-panel>
<m3e-expansion-panel hide-toggle> <span slot="header">Panel header</span> Panel contents </m3e-expansion-panel>
Use a to create a coordinated group of panels.
<m3e-accordion> <m3e-expansion-panel open> <span slot="header">Panel 1</span> I am content for the first panel </m3e-expansion-panel> <!-- Additional panels omitted for brevity --> </m3e-accordion>
By default, only one panel can be open at a time. Use the attribute to allow multiple panels to be open at the same time.
<m3e-accordion multi> <m3e-expansion-panel open> <span slot="header">Panel 1</span> I am content for the first panel </m3e-expansion-panel> <!-- Additional panels omitted for brevity --> </m3e-accordion>
Expansion panels imitate the experience of the native and elements. Internally, a panel's header is given ARIA and the attribute with the panel's internal content wrapper's ID. Additionally, the internal header's attribute reflects the expanded state of the panel.
Because expansion panel headers are buttons, avoid adding interactive controls in the slot, including buttons and anchors.
The component presents a dynamic menu of related actions, elegantly revealed from a floating action button (FAB). See
for more information on using floating action buttons.
The is used in conjunction with a to create a menu opened when a FAB is activated.
Use a , nested inside a , to trigger a menu.
Use the attribute of the to designate the menu to open.
Nest 2-6 components inside a to designate menu items.
<m3e-fab variant="primary" size="large"> <m3e-fab-menu-trigger for="fabmenu"> <m3e-icon name="edit"></m3e-icon> </m3e-fab-menu-trigger> </m3e-fab> <m3e-fab-menu id="fabmenu"> <m3e-fab-menu-item> <m3e-icon slot="icon" name="email" filled></m3e-icon> Email </m3e-fab-menu-item> <m3e-fab-menu-item> <m3e-icon slot="icon" name="chat" filled></m3e-icon> Message </m3e-fab-menu-item> <m3e-fab-menu-item> <m3e-icon slot="icon" name="folder" filled></m3e-icon> Folder </m3e-fab-menu-item> </m3e-fab-menu>
The supports three appearance variants: (default), , and . Use the attribute to change the appearance.
<m3e-fab variant="secondary" size="large"> <m3e-fab-menu-trigger for="fabmenu"> <m3e-icon name="edit"></m3e-icon> </m3e-fab-menu-trigger> </m3e-fab> <m3e-fab-menu id="fabmenu" variant="secondary"> <!-- Items omitted for brevity --> </m3e-fab-menu> <!-- Other variants omitted for brevity -->
Use the slot of a to present an icon before an item's label. While not required, the following example uses the component from to present icons using Material Symbols. See
You can disable a using the attribute. In addition, if the is disabled, the will not be opened on click.
<m3e-fab-menu-item disabled> <m3e-icon slot="icon" name="email" filled></m3e-icon> Email </m3e-fab-menu-item>
Menu items can also behave like links by specifying a URL using the attribute. You can also specify , and similar to native anchor tags.
<m3e-fab-menu-item href="https://www.google.com" target="_blank"> <m3e-icon slot="icon" name="open_in_new_window" filled></m3e-icon> Google </m3e-fab-menu-item>
FAB menus are given ARIA and items are given indicating that the menu behaves like a contextual action list.
The attribute of the is assigned the ID of the menu and indicates that the FAB will open a menu. In addition, the FAB's attribute reflects the opened state of the menu.
When is specified for an item, the ARIA role does not change and continues to be . In addition, the component presents an internal anchor, hidden from assistive technologies, used to invoke native browser features such a context menu options and tooltips.
You also need a module script for due to being a dependency.
The is a floating action button (FAB) used to present important actions.
There are seven FAB appearance variants: , (default), , and , , , and . Use the attribute to change the appearance.
<m3e-fab variant="primary"> <m3e-icon name="add"></m3e-icon> </m3e-fab> <!-- Additional variants omitted for brevity -->
Use the attribute to lower the elevation of a FAB to reduce emphasis. Lowering is supported for all appearance variants.
<m3e-fab lowered variant="primary"> <m3e-icon name="add"></m3e-icon> </m3e-fab> <!-- Additional variants omitted for brevity -->
There are three size variants which can be used to add or remove emphasis: , (default), and . Use the attribute to control a FAB's size.
<m3e-fab size="small"> <m3e-icon name="add"></m3e-icon> </m3e-fab> <!-- Additional variants omitted for brevity -->
<m3e-fab disabled> <m3e-icon name="add"></m3e-icon> </m3e-fab> <m3e-fab disabled-interactive> <m3e-icon name="add"></m3e-icon> </m3e-fab>
Use the attribute and slot to create an extended FAB.
<m3e-fab extended size="small"> <m3e-icon name="add"></m3e-icon> <span slot="label">Extended</span> </m3e-fab> <!-- Additional sizes omitted for brevity -->
Use the attribute to control the behavior of a FAB. By default, is . You can change this to or to submit or reset a parenting form when clicked. When submitting a form, you can use the and attributes to specify a value.
FABs can also behave like links by specifying a URL using the attribute. You can also specify , and similar to native anchor tags.
<m3e-fab extended href="https://www.google.com" target="_blank"> <m3e-icon name="open_in_new_window"></m3e-icon> <span slot="label">Google</span> </m3e-fab>
FABs are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. FABs support negative density scales down to -3, with each step reducing the visual height by one density unit. Regardless of visual size, FABs maintain a 48x48dp minimum touch target to ensure reliable accessibility.
The following illustrates applying density to the and sizes.
By default, FABs are given the ARIA role , indicating they are clickable elements that trigger actions.
Unless extended, always provide an accessible label via or for each FAB.
When disabling using either the or attribute, is used to convey to assistive technologies that the FAB is disabled. If disabled using , is retained to convey that the FAB continues to be interactive.
When is specified, the ARIA role is used instead of to convey the FAB provides an interactive reference to a resource. In addition, the component presents an internal anchor, hidden from assistive technologies, used to invoke native browser features such a context menu options and tooltips.
The is a container for form controls that applies Material Design styling and behavior. Supported controls include: , , , and .
There are two form field appearance variants: (default) and . Use the attribute to change the appearance.
<m3e-form-field variant="outlined"> <label slot="label" for="fld">Outlined</label> <input id="fld" /> </m3e-form-field> <!-- Additional variants omitted for brevity -->
When a form field's control is empty, the label is presented over the control instead of above. You can change this behavior using the attribute. Supported values include and (default).
<m3e-form-field float-label="always"> <label slot="label" for="fld">Always float label</label> <input id="fld" /> </m3e-form-field>
Hint labels are additional descriptive text that appear in a field's subscript. Use the slot to specify a hint. By default, a field's subscript is hidden until focused or when a field is invalid. Use the attribute to control this behavior. Supported values include: , (default), and . Using will remove the space reserved for a field's subscript.
<m3e-form-field hide-subscript="never"> <label slot="label" for="fld">Field w/ hint</label> <input id="fld" /> <span slot="hint">Hint text</span> </m3e-form-field>
Errors are hidden by default and only become visible after the user has interacted with the field—such as focusing and then blurring—or after the parent form has been submitted. This behavior ensures that validation feedback is contextually relevant and non-intrusive, appearing only when the user is likely to expect it.
When an error message is shown, it occupies the same subscript region beneath the field that is normally used for the hint label. To prevent visual overlap and maintain clarity, the hint is automatically hidden whenever an error is displayed.
<m3e-form-field> <label slot="label" for="fld5">Required field</label> <input id="fld5" required /> <span slot="hint">Hint text</span> </m3e-form-field>
By default, the validation message of a field's control is presented in a field's subscript. You can specify custom error messages using the slot.
<m3e-form-field> <label slot="label" for="fld">Required field</label> <input id="fld" required /> <span slot="hint">Hint text</span> <span slot="error">Custom error message</span> </m3e-form-field>
By default, required fields will present a marker next to the field's label. Use the attribute to remove it.
<m3e-form-field hide-required-marker> <label slot="label" for="fld">Required field</label> <input id="fld" required /> </m3e-form-field>
Content can be included before or after the field's control to serve as a prefix or suffix. This content is rendered inside the same visual container that wraps the form control, ensuring consistent alignment, spacing, and styling.
Use the and slots for icons and buttons. For textual content, use the and slots.
<m3e-form-field> <label slot="label" for="fld">Amount</label> <span slot="prefix-text">$</span> <input id="fld" type="number" placeholder="0" /> <span slot="suffix-text">.00</span> <m3e-icon-button slot="suffix"> <m3e-icon name="clear"></m3e-icon> </m3e-icon-button> </m3e-form-field>
Form fields are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Form fields support negative density scales down to -3, with each step reducing the visual height by one density unit.
<m3e-form-field variant="outlined" class="density-3"> <label slot="label" for="fld">Density -3</label> <input id="fld" /> </m3e-form-field> <!-- Additional fields and variants omitted for brevity -->
The will hide hint labels and error messages from the accessibility tree and use a visually hidden element to present them to assistive technologies. Their IDs are added to the attribute of the field's control.
The component provides expressive, accessible headings for pages and sections
The supports five variants: (default), , , and . It supports three size variants: , (default) and . Use the and attributes to change the typescale of a heading.
<m3e-heading variant="display" size="large">Display Large</m3e-heading> <m3e-heading variant="display" size="medium">Display Medium</m3e-heading> <m3e-heading variant="display" size="small">Display Small</m3e-heading> <!-- Additional variants omitted for brevity -->
Use the attribute to control whether a heading's typescale is emphasized.
<m3e-heading variant="display" size="large" emphasized>Display Large</m3e-heading> <m3e-heading variant="display" size="medium" emphasized>Display Medium</m3e-heading> <m3e-heading variant="display" size="small" emphasized>Display Small</m3e-heading> <!-- Additional variants omitted for brevity -->
By default, headings are not given an ARIA role, indicating that they are treated as neutral, non-semantic elements in the accessibility tree unless explicitly referenced or annotated.
Use the attribute to control whether a heading adopts the ARIA and attributes. When is set, is applied and reflects the attribute's value. Supported values range from to , aligning with semantic heading levels.
The component is an icon button users interact with to perform a supplementary action.
There are four icon button appearance variants: , , , and (default). Use the attribute to change the appearance.
<m3e-icon-button variant="filled"> <m3e-icon name="check"></m3e-icon> </m3e-icon-button> <!-- Additional variants omitted for brevity -->
Icon buttons can come in two shapes: (default) or . Use the attribute to control the shape of a button.
<m3e-icon-button variant="filled" shape="square"> <m3e-icon name="check"></m3e-icon> </m3e-icon-button> <!-- Additional variants omitted for brevity -->
There are five size variants which can be used to add or remove emphasis: , (default), , , and . Use the attribute to control an icon button's size.
<m3e-icon-button variant="tonal" size="extra-small"> <m3e-icon name="add"></m3e-icon> </m3e-icon-button> <!-- Additional sizes omitted for brevity -->
There are three width variants which can be used to add or remove emphasis: (default), , and . Use the attribute to control an icon button's width.
<m3e-icon-button variant="tonal" width="narrow"> <m3e-icon name="add"></m3e-icon> </m3e-icon-button> <m3e-icon-button variant="tonal" width="wide"> <m3e-icon name="add"></m3e-icon> </m3e-icon-button>
Use the attribute to control whether an icon button can be used for binary selection and the attribute to control the selected state. When clicked, a cancellable event is emitted followed by an and event. To cancel selection change, call during .
<m3e-icon-button variant="filled" toggle> <m3e-icon name="check"></m3e-icon> </m3e-icon-button> <!-- Additional variants omitted for brevity -->
You can also change the icon presented for a selected toggle button using the slot.
<m3e-icon-button variant="tonal" toggle> <m3e-icon name="close"></m3e-icon> <m3e-icon slot="selected" name="check"></m3e-icon> </m3e-icon-button>
When selected, icon buttons change shape. Rounded buttons become square and square buttons become rounded. The following illustrates the selected states for both rounded and square icon buttons.
<m3e-icon-button variant="filled" disabled> <m3e-icon name="check"></m3e-icon> </m3e-icon-button> <m3e-icon-button variant="filled" disabled-interactive> <m3e-icon name="check"></m3e-icon> </m3e-icon-button>
Use the attribute to control the behavior of an icon button. By default, is . You can change this to or to submit or reset a parenting form when clicked. When submitting a form, you can use the and attributes to specify a value.
Icon buttons can also behave like links by specifying a URL using the attribute. You can also specify , and similar to native anchor tags.
Icon buttons are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Buttons support negative density scales down to -3, with each step reducing the visual height by one density unit. Regardless of visual size, icon buttons maintain a 48x48dp minimum touch target to ensure reliable accessibility.
The following illustrates applying density to the and sized icon buttons.
<m3e-icon-button class="density-3" variant="filled" size="extra-small"> <m3e-icon name="check"></m3e-icon> </m3e-icon-button> <!-- Additional buttons omitted for brevity -->
By default, icon buttons are given the ARIA role , indicating they are clickable elements that trigger actions.
Always provide an accessible label via or for each icon button.
When disabling using either the or attribute, is used to convey to assistive technologies that the icon button is disabled. If disabled using , is retained to convey that the icon button continues to be interactive.
When is specified, the ARIA role is used instead of to convey the icon button provides an interactive reference to a resource. In addition, the component presents an internal anchor, hidden from assistive technologies, used to invoke native browser features such a context menu options and tooltips.
For toggle icon buttons, is used to convey the selected state, even when disabled.
The component makes it easy to use Material Symbols in your application. It supports outlined, rounded, and sharp variants, as well as variable font features like fill, weight, grade, and optical size. For more information, see the
The Material Symbols font is the easiest way to incorporate Material Symbols into your application. Using the
, you can use variable fonts to optimize icon usage within your application. See
to determine whether your user's browser supports variable fonts.
Alternatively, the package provides SVG versions of Material Symbols that register automatically with . See the SVG Icons section below for usage details.
Or register an entire variant family at once:
Each icon module performs a side-effect registration with the internal registry used by . Once registered, the icon becomes available for rendering. If an icon is not registered, it will fall back to the Material Symbols variable font.
Icons are given the ARIA , indicating that they represent meaningful visual content and should be interpreted by assistive technologies as images.
Include if the icon is purely decorative. Otherwise, they must be labeled using or .
The module provides expressive, accessible components for organizing and displaying lists of items.
To create a list of items, use the component with one or more components nested inside it.
— Renders the content of the list item.
— Renders the leading content of the list item.
— Renders the overline of the list item.
— Renders the supporting text of the list item.
— Renders the trailing content of the list item.
<m3e-list> <m3e-list-item> <m3e-icon slot="leading" name="person"></m3e-icon> <span slot="overline">Overline</span> Headline <span slot="supporting-text">Supporting text</span> <span slot="trailing">100+</span> </m3e-list-item> </m3e-list>
There are two appearance variants (default) and . Use the attribute to control a list's appearance.
<m3e-list> <m3e-list-item>Item 1</m3e-list-item> <!-- Additional items omitted for brevity --> </m3e-list>
<m3e-list variant="segmented"> <m3e-list-item>Item 1</m3e-list-item> <!-- Additional items omitted for brevity --> </m3e-list>
List items are responsive containers that automatically adapt their height and internal spacing according to the item's line count, ensuring correct layout for one-line, two-line, and three-line configurations.
<m3e-list> <m3e-list-item>Label text</m3e-list-item> <m3e-list-item> Label text <span slot="supporting-text">Supporting text</span> </m3e-list-item> <m3e-list-item> <span slot="overline">Overline</span> Label text <span slot="supporting-text">Supporting text</span> </m3e-list-item> </m3e-list>
List items automatically size and position leading and trailing media—such as videos, images, avatars, and icons—according to the item's detected line count, reserving space when needed to keep visuals consistently aligned across one-, two-, and three-line configurations.
<m3e-list> <m3e-list-item> <m3e-icon slot="leading" name="stars"></m3e-icon> Label text <m3e-icon slot="trailing" name="arrow_right"></m3e-icon> </m3e-list-item> <!-- Additional items omitted for brevity --> </m3e-list>
<m3e-list> <m3e-list-item> <m3e-avatar slot="leading">AB</m3e-avatar> Label text <m3e-icon slot="trailing" name="arrow_right"></m3e-icon> </m3e-list-item> <!-- Additional items omitted for brevity --> </m3e-list>
<m3e-list> <m3e-list-item> <img slot="leading" src="[YOUR IMAGE URL HERE]" /> Label text <m3e-icon slot="trailing" name="arrow_right"></m3e-icon> </m3e-list-item> <!-- Additional items omitted for brevity --> </m3e-list>
<m3e-list> <m3e-list-item> <video slot="leading" autoplay loop poster="[YOUR IMAGE URL HERE]" preload="auto"> <source src="[YOUR VIDEO URL HERE]" type="video/mp4"/> </video> Label text <m3e-icon slot="trailing" name="arrow_right"></m3e-icon> </m3e-list-item> <!-- Additional items omitted for brevity --> </m3e-list>
Dividers are only supported for the standard variant and are automatically hidden for segmented lists.
Dividers can also be inset using the attribute of a .
<m3e-list> <m3e-list-item>Item 1</m3e-list-item> <m3e-divider inset></m3e-divider> <!-- Additional items omitted for brevity --> </m3e-list>
Use the and to create a list where each item performs a single action. Action lists support the same appearance variants as and supports the same slots as .
<m3e-action-list> <m3e-list-action>Action 1</m3e-list-action> <!-- Additional actions omitted for brevity --> </m3e-action-list>
<m3e-action-list variant="segmented"> <m3e-list-action>Action 1</m3e-list-action> <!-- Additional actions omitted for brevity --> </m3e-action-list>
Individual list actions can be disabled using the attribute.
<m3e-action-list> <m3e-list-action disabled>Disabled action 1</m3e-list-action> <!-- Additional actions omitted for brevity --> </m3e-action-list>
<m3e-action-list variant="segmented"> <m3e-list-action disabled>Disabled action 1</m3e-list-action> <!-- Additional actions omitted for brevity --> </m3e-action-list>
List actions can also behave like links by specifying a URL using the attribute. You can also specify , and similar to native anchor tags.
Both and can use an to present hierarchical content that can be expanded or collapsed. The provides a disclosure-style header that toggles the visibility of a nested list, allowing related actions or items to be grouped without overwhelming the parent list.
Use the attribute to control whether an expandable list item is open.
<m3e-action-list> <m3e-expandable-list-item open> <m3e-icon slot="leading" name="stars"></m3e-icon> List item <div slot="items"> <m3e-list-action> <m3e-icon slot="leading" name="stars"></m3e-icon> List item </m3e-list-action> <!-- Additional actions omitted for brevity --> </div> </m3e-expandable-list-item> <!-- Additional items omitted for brevity --> </m3e-action-list>
<m3e-action-list variant="segmented"> <m3e-expandable-list-item open> <m3e-icon slot="leading" name="stars"></m3e-icon> List item <div slot="items"> <m3e-list-action> <m3e-icon slot="leading" name="stars"></m3e-icon> List item </m3e-list-action> <!-- Additional actions omitted for brevity --> </div> </m3e-expandable-list-item> <!-- Additional items omitted for brevity --> </m3e-action-list>
Use the and to create a list for selecting values, where each item performs a single option. Selection lists support the same appearance variants as and supports the same slots as . However, the selection indicator will replaces content in the slot unless selection indicators are hidden.
<m3e-selection-list> <m3e-list-option>Option 1</m3e-list-option> <!-- Additional options omitted for brevity --> </m3e-selection-list>
<m3e-selection-list variant="segmented"> <m3e-list-option>Option 1</m3e-list-option> <!-- Additional options omitted for brevity --> </m3e-selection-list>
Selection lists support both single-select and multi-select modes. Use the attribute to allow multiple options to be selected simultaneously.
<m3e-selection-list multi> <m3e-list-option>Option 1</m3e-list-option> <!-- Additional options omitted for brevity --> </m3e-selection-list>
<m3e-selection-list variant="segmented" multi> <m3e-list-option>Option 1</m3e-list-option> <!-- Additional options omitted for brevity --> </m3e-selection-list>
Use the attribute of to hide selection indicators. If an option has an icon via use of the slot, the icon is presented.
<m3e-selection-list multi hide-selection-indicator> <m3e-list-option selected>Option 1<m3e-icon slot="trailing" name="bookmark"></m3e-icon></m3e-list-option> <m3e-list-option>Option 2<m3e-icon slot="trailing" name="bookmark"></m3e-icon></m3e-list-option> <m3e-list-option>Option 3<m3e-icon slot="trailing" name="bookmark"></m3e-icon></m3e-list-option> </m3e-selection-list>
Individual list options can be disabled using the attribute and can be selected and disabled.
<m3e-selection-list multi> <m3e-list-option disabled>Option 1</m3e-list-option> <m3e-list-option selected disabled>Option 1</m3e-list-option> <!-- Additional options omitted for brevity --> </m3e-selection-list>
<m3e-selection-list variant="segmented" multi> <m3e-list-option disabled>Option 1</m3e-list-option> <m3e-list-option selected disabled>Option 1</m3e-list-option> <!-- Additional options omitted for brevity --> </m3e-selection-list>
Selection lists can participate in form submission as a
(FACE), contributing its state to the form data. The attribute defines the key under which the component's value is submitted in a form. The list's value is determined by selected options. You can use the of a to control a option's value. When multiple options are selected, form data will include multiple entries keyed by the same .
The component honors the state (on the option itself) as a
Lists are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Lists support negative density scales down to -3, with each step reducing the visual height by one density unit.
The following illustrates -3 density applied to a segmented, multi-select list.
<m3e-selection-list class="density-3" variant="segmented" multi> <!-- Options omitted for brevity --> </m3e-selection-list>
Lists are given the ARIA and list items are given to ensure that assistive technologies can correctly interpret the structure and semantics of the content.
Action lists are given the ARIA and list actions are given to ensure that assistive technologies can correctly interpret the structure and semantics of the content. Each list action includes a nested interactive element with ARIA . When is specified, the ARIA role is used instead of to convey the list action provides an interactive reference to a resource. In addition, the component presents an internal anchor, hidden from assistive technologies, used to invoke native browser features such a context menu options and tooltips. When disabled using the attribute, is applied to the nested interactive element in order to convey to assistive technologies that the action is disabled.
Selection lists are given the ARIA and options are given to ensure that assistive technologies recognize the widget as a selectable list and expose the correct interaction model to users. The attribute reflects whether an option is currently selected. When an option is selected, is exposed to assistive technologies; otherwise, it is . When disabled using the attribute, is applied to the option in order to convey to assistive technologies that the option is disabled. When multi-select is enabled through the attribute, is applied to the selection list indicating multiple options can be selected.
Expandable list items use ARIA and emulate the behavior of the native and elements. Inside each item, the header is assigned along with an attribute referencing the item's internal content wrapper. The content wrapper is given to represent the nested list of child items. The header's attribute reflects the current expanded or collapsed state of the item.
The component shows indeterminate progress for a short wait time. It uses animation to grab attention, mitigate perceived latency, and indicate that an activity is in progress.
The implements the ARIA pattern. By default, the component sets to 0 and to 100.
Always provide an accessible label via or for each loading indicator.
The component presents a list of choices on a temporary surface, typically anchored to a trigger element.
The is used in conjunction with a to create a menu opened when the parenting element of the trigger is activated. Use the attribute of the to designate the menu to open.
<m3e-button> <m3e-menu-trigger for="menu">Menu</m3e-menu-trigger> </m3e-button> <m3e-menu id="menu"> <m3e-menu-item>Item 1</m3e-menu-item> <m3e-menu-item>Item 2</m3e-menu-item> </m3e-menu>
<!-- Trigger omitted for brevity --> <m3e-menu id="menu" variant="vibrant"> <m3e-menu-item>Item 1</m3e-menu-item> <m3e-menu-item>Item 2</m3e-menu-item> </m3e-menu>
The component supports three distinct types of menu items, each tailored for different interaction patterns and semantic roles:
— A standard menu item typically used for navigation or triggering commands.
— A toggleable menu item option used for multi-select scenarios.
— A toggleable menu item option used for single-select scenarios.
All menu item types provide two named slots for icons. Use the slot to present an icon before an item's label and use the slot to present an icon after it.
<!-- Trigger omitted for brevity --> <m3e-menu id="menu"> <m3e-menu-item> <m3e-icon slot="icon" name="dialpad"></m3e-icon> Redial </m3e-menu-item> <m3e-menu-item> <m3e-icon slot="icon" name="voicemail"></m3e-icon> Check voice mail </m3e-menu-item> <m3e-menu-item> <m3e-icon slot="icon" name="notifications_off"></m3e-icon> Disable alerts </m3e-menu-item> </m3e-menu>
<!-- Trigger omitted for brevity --> <m3e-menu id="menu"> <m3e-menu-item disabled> <m3e-icon slot="icon" name="dialpad"></m3e-icon> Redial </m3e-menu-item> <!-- Additional items omitted for brevity --> </m3e-menu>
Use a to present a toggleable menu item that supports multi-select. The attribute reflects the checked state of the menu item. If a menu item specifies an icon using the slot, it will be replaced with a check icon when checked.
<!-- Trigger omitted for brevity --> <m3e-menu id="menu"> <m3e-menu-item-checkbox checked> <m3e-icon slot="icon" name="format_bold"></m3e-icon> Bold </m3e-menu-item-checkbox> <m3e-menu-item-checkbox> <m3e-icon slot="icon" name="format_italic"></m3e-icon> Italic </m3e-menu-item-checkbox> <m3e-menu-item-checkbox> <m3e-icon slot="icon" name="format_underlined"></m3e-icon> Underline </m3e-menu-item-checkbox> </m3e-menu>
Use a to present a toggleable menu item that supports single-select. The attribute reflects the checked state of the menu item. If a menu item specifies an icon using the slot, it will be replaced with a check icon when checked.
<!-- Trigger omitted for brevity --> <m3e-menu id="menu"> <m3e-menu-item-radio checked>Ascending</m3e-menu-item-radio> <m3e-menu-item-radio>Descending</m3e-menu-item-radio> </m3e-menu>
Use a to organize multiple sets of radio items within a single . Each group maintains its own selection state, allowing distinct radio groups to coexist independently.
<!-- Trigger omitted for brevity --> <m3e-menu id="menu"> <m3e-menu-item-group> <m3e-menu-item-radio checked>Ascending</m3e-menu-item-radio> <m3e-menu-item-radio>Descending</m3e-menu-item-radio> </m3e-menu-item-group> <m3e-divider></m3e-divider> <m3e-menu-item-group> <m3e-menu-item-radio checked>Alphabetical</m3e-menu-item-radio> <m3e-menu-item-radio>By Date</m3e-menu-item-radio> </m3e-menu-item-group> </m3e-menu>
Submenus are triggered by nesting a in a and designating the submenu to open using the attribute.
<m3e-button> <m3e-menu-trigger for="menu1">Menu with submenus</m3e-menu-trigger> </m3e-button> <m3e-menu id="menu1"> <m3e-menu-item> <m3e-menu-trigger for="submenu1">Fruits with A</m3e-menu-trigger> </m3e-menu-item> <m3e-menu-item>Grapes</m3e-menu-item> <m3e-menu-item>Olive</m3e-menu-item> <m3e-menu-item>Orange</m3e-menu-item> </m3e-menu> <m3e-menu id="submenu1"> <m3e-menu-item>Apricot</m3e-menu-item> <m3e-menu-item>Avocado</m3e-menu-item> <m3e-menu-item> <m3e-menu-trigger for="submenu2">Apples</m3e-menu-trigger> </m3e-menu-item> </m3e-menu> <m3e-menu id="submenu2"> <m3e-menu-item>Fuji</m3e-menu-item> <m3e-menu-item>Granny Smith</m3e-menu-item> <m3e-menu-item>Red Delicious</m3e-menu-item> </m3e-menu>
Menus are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Menus support negative density scales down to -3, with each step reducing the visual height by one density unit.
<!-- Trigger omitted for brevity --> <m3e-menu id="menu" class="density-3"> <m3e-menu-item> <m3e-icon slot="icon" name="stars"></m3e-icon> Item 1 </m3e-menu-item> <!-- Additional items omitted for brevity --> </m3e-menu> <!-- Additional menus omitted for brevity -->
Menus are given the ARIA , indicating that the serve as a container for a list of interactive menu items.
Triggers augment their parenting elements with , , and to create a relationship with the trigger's menu.
When a menu is opened, the focus is set to the first enabled menu item. On close, focus is returned to the trigger.
Standard menu items are given ARIA , checkboxes are given , and radios are given . The attribute reflects the checked state of an item.
Disabled menu items are given in order to convey to assistive technologies that the item is disabled. In addition, is removed to indicate the item is no longer focusable.
Menu item groups are given ARIA and should be labelled using or .
The is a horizontal bar, typically used on smaller devices, that allows a user to switch between 3-5 views.
The supports three modes: (default), , and . In a compact mode, items are oriented vertically and fill the available space of the bar. When extended, items are oriented horizontally and are centered within the bar. Using , the orientation is responsive and determined by viewport size. Use the attribute to change a bar's mode.
The following example illustrates both compact and expanded modes.
<m3e-nav-bar mode="compact"> <m3e-nav-item><m3e-icon slot="icon" name="news"></m3e-icon>News</m3e-nav-item> <m3e-nav-item><m3e-icon slot="icon" name="globe"></m3e-icon>Global</m3e-nav-item> <m3e-nav-item><m3e-icon slot="icon" name="star"></m3e-icon>For you</m3e-nav-item> <m3e-nav-item><m3e-icon slot="icon" name="newsstand"></m3e-icon>Trending</m3e-nav-item> </m3e-nav-bar> <br /> <m3e-nav-bar mode="expanded"> <!-- Items omitted for brevity --> </m3e-nav-bar>
The only supports single-select. Use the attribute of to specify which item in a bar is selected. When clicked, a cancellable event is emitted followed by an and event. To cancel selection change, call during .
<m3e-nav-bar> <m3e-nav-item selected><m3e-icon slot="icon" name="news"></m3e-icon>News</m3e-nav-item> <!-- Additional items omitted for brevity --> </m3e-nav-bar>
<m3e-nav-bar> <m3e-nav-item disabled><m3e-icon slot="icon" name="news"></m3e-icon>News</m3e-nav-item> <m3e-nav-item disabled-interactive><m3e-icon slot="icon" name="globe"></m3e-icon>Global</m3e-nav-item> <!-- Additional items omitted for brevity --> </m3e-nav-bar>
<m3e-nav-bar> <m3e-nav-item href="https://www.google.com" target="_blank"> <m3e-icon slot="icon" name="news"></m3e-icon>News </m3e-nav-item> </m3e-nav-bar>
Navigation bars are given ARIA , indicating that the region serves as a navigational landmark and helps assistive technologies identify it as a container for site or app navigation links. Always provide an accessible label via or .
The component is a hierarchical menu optimized for sidebars and drawers on larger devices. It enables users to explore nested menu structures and seamlessly switch between application views.
Use the and to create a multilevel navigation menu.
Use the slot of a to specify an item's label. In addition, use the slot to include an icon before the item's label and use the slot to specify to specify a compact status indicator or supplementary text.
To create multiple levels, nest one or more components.
<m3e-nav-menu> <m3e-nav-menu-item open> <m3e-icon slot="icon" name="rocket_launch" aria-hidden="true"></m3e-icon> <span slot="label">Getting Started</span> <m3e-nav-menu-item> <m3e-icon slot="icon" name="widgets" aria-hidden="true"></m3e-icon> <span slot="label">Overview</span> </m3e-nav-menu-item> <!-- Additional items omitted for brevity -->; </m3e-nav-menu-item> <m3e-nav-menu-item> <span slot="label">Actions</span> <!-- Items omitted for brevity -->; </m3e-nav-menu-item> </m3e-nav-menu>
You can use a to group top-level items in a menu. Use the slot to specify a group's label. Use a to separate groups in a menu. See
<m3e-nav-menu> <m3e-nav-menu-item-group> <m3e-heading slot="label" variant="label" size="large">Mail</m3e-heading> <m3e-nav-menu-item> <m3e-icon slot="icon" name="mail"></m3e-icon> <span slot="label">Inbox</span> <span slot="badge">24</span> </m3e-nav-menu-item> <!-- Additional items omitted for brevity --> </m3e-nav-menu-item-group> <m3e-divider></m3e-divider> <m3e-nav-menu-item-group> <m3e-heading slot="label" variant="label" size="large">Personal folders</m3e-heading> <!-- Items omitted for brevity -->; </m3e-nav-menu-item-group> </m3e-nav-menu>
Use the attribute of a to disable the item and any children. Disabling an item with children will not automatically close it.
<m3e-nav-menu> <m3e-nav-menu-item open disabled> <m3e-icon slot="icon" name="rocket_launch" aria-hidden="true"></m3e-icon> <span slot="label">Getting Started</span> <m3e-nav-menu-item> <m3e-icon slot="icon" name="widgets" aria-hidden="true"></m3e-icon> <span slot="label">Overview</span> </m3e-nav-menu-item> <!-- Additional items omitted for brevity -->; </m3e-nav-menu-item> <m3e-nav-menu-item open> <span slot="label">Actions</span> <m3e-nav-menu-item disabled><span slot="label">Button</span></m3e-nav-menu-item> <!-- Additional items omitted for brevity -->; </m3e-nav-menu-item> </m3e-nav-menu>
Nav menus are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Nav menus support negative density scales down to -3, with each step reducing the visual height by one density unit.
The following illustrates applying a -3 density to nav menu.
<m3e-nav-menu class="density-3"> <!-- Items omitted for brevity -->; </m3e-nav-menu>
Nav menus are given ARIA , and items are assigned , indicating a structured, hierarchical navigation model that supports keyboard interaction, semantic grouping, and accessible labeling. Expandable items use to indicate visibility of nested content, and current items may use to signal the active page or view.
Groups are given ARIA , indicating that their contents form a semantic unit within the navigation tree and are labeled by the content of the group's slot.
Icons and dividers are given ensure they're treated as purely decorative and excluded from the accessibility tree.
to provide a vertical bar, typically used on larger devices, that allows a user to switch between views.
The supports three modes: (default), , and . In a compact mode, items are oriented vertically. When expanded, items are oriented horizontally. Using , the orientation is responsive and determined by viewport size. Use the attribute to change a bar's mode.
A is used in conjunction with the to provide a toggle button used to toggle between modes. The attribute designates the rail to toggle.
An extended will automatically transition to reflect the mode of the rail.
<m3e-nav-rail id="nav-rail"> <m3e-icon-button toggle> <m3e-icon name="menu"></m3e-icon> <m3e-icon slot="selected" name="menu_open"></m3e-icon> <m3e-nav-rail-toggle for="nav-rail"></m3e-nav-rail-toggle> </m3e-icon-button> <m3e-fab size="small"> <m3e-icon name="edit"></m3e-icon> <span slot="label">Extended</span> </m3e-fab> <m3e-nav-item><m3e-icon slot="icon" name="news"></m3e-icon>News</m3e-nav-item> <m3e-nav-item><m3e-icon slot="icon" name="globe"></m3e-icon>Global</m3e-nav-item> <m3e-nav-item><m3e-icon slot="icon" name="star"></m3e-icon>For you</m3e-nav-item> <m3e-nav-item><m3e-icon slot="icon" name="newsstand"></m3e-icon>Trending</m3e-nav-item> </m3e-nav-rail>
The only supports single-select. Use the attribute of to specify which item in a rail is selected. When clicked, a cancellable event is emitted followed by an and event. To cancel selection change, call during .
Navigation rails are given ARIA , indicating that the region serves as a navigational landmark and helps assistive technologies identify it as a container for site or app navigation links. Always provide an accessible label via or .
Rail toggles use to associate its parenting element with the rail, and dynamically update to indicate whether the rail is expanded.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js", "@m3e/web/core/layout": "/node_modules/@m3e/web/dist/core-layout.js", "@m3e/web/nav-bar": "/node_modules/@m3e/web/nav-bar.js" } } </script>
The component is a compact, accessible paginator control for navigating paged data sets. It provides semantic first/previous/next/last navigation controls and an optional page-size selector.
At minimum, you must specify the total number of items being paged and the page size. Use the and attributes of the component, respectively. By default, is set to 50.
When a user interacts with the paginator, it emits a event. This event can be used to update any associated data view and includes the following properties:
— The zero-based index of the current page.
(optional) — The zero-based index of the previously selected page.
— The total number of items being paged.
The attribute lets you define which page size options are available to users. It accepts a comma-separated list of values and may include . By default, is set to .
The current will always appear in the dropdown, event if its not included in .
When is set to , the range label and navigation actions are hidden.
By default, the first and last page buttons are hidden. Use the attribute to show them.
Paginators are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Paginators support negative density scales down to -3, with each step reducing the visual height by one density unit. The internal form field accepts compact densities (-3 and -2) and ignores larger values.
The following illustrates applying -3 density to a paginator.
The paginator uses ARIA to semantically group its child controls. You must add an or attribute to each with a label that describes the content controlled by the pagination control.
You can use the , , , and attributes to change the accessible labels of navigation actions.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js", "@m3e/web/core/anchoring": "/node_modules/@m3e/web/dist/core-anchoring.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js", "@m3e/web/core/platform": "/node_modules/@m3e/web/dist/core-platform.js", "@m3e/web/form-field": "/node_modules/@m3e/web/dist/form-field.js", "@m3e/web/select": "/node_modules/@m3e/web/dist/select.js", "@m3e/web/option": "/node_modules/@m3e/web/dist/option.js", "@m3e/web/icon-button": "/node_modules/@m3e/web/dist/icon-button.js", "@m3e/web/tooltip": "/node_modules/@m3e/web/dist/tooltip.js" } } </script>
The and components provide accessible, animated progress indicators for tracking the completion of tasks or processes.
By default, circular progress indicators are determinate. Use the attribute to indicate completion percentage and use to specify the total number of steps or units of progress. In addition, you can provide content in the default slot.
Use the attribute to change the mode to indeterminate. In this mode, and are ignored.
Use the to present a horizontal bar for indicating progress and activity. This component supports four modes: (default), , , and .
Use the attribute to change the mode. As with a circular indicator, use to set the appearance to either (default) or .
Use the mode where the percentage of the operation complete is known. Use the attribute to indicate completion percentage and use to specify the total number of steps or units of progress. This is the default mode.
When using the variant, the indicator transitions to the flat appearance automatically when progress reaches 100%.
Use the mode to express that something is happening, but without conveying discrete progress. In this mode, and are ignored.
Use the mode to indicate some activity or loading from the server. In this mode, determines the progress of the primary bar while the attribute is used to show buffering progress.
As with mode, when using the variant, the indicator transitions to the flat appearance automatically when progress reaches 100%.
<m3e-linear-progress-indicator mode="buffer" value="10" buffer-value="25"></m3e-linear-progress-indicator> <m3e-linear-progress-indicator variant="wavy" mode="buffer" value="10" buffer-value="25"></m3e-linear-progress-indicator>
Use the mode to indicate pre-loading before the actual loading starts. In this mode, and are ignored.
Progress indicators implement the ARIA pattern. They set to , to , and to .
Always provide an accessible label via or for each progress indicator.
The and components enable single-choice selection within a set of mutually exclusive options.
<m3e-radio-group> <label><m3e-radio value="1"></m3e-radio> Option 1</label> <label><m3e-radio value="2"></m3e-radio> Option 2</label> <label><m3e-radio value="3"></m3e-radio> Option 3</label> <label><m3e-radio value="4"></m3e-radio> Option 4</label> </m3e-radio-group>
Associate a label with a radio using the element. In addition, radio groups can also be labelled using a .
<label for="radio-group">Select an option</label> <m3e-radio-group id="radio-group"> <label><m3e-radio value="1"></m3e-radio> Option 1</label> <!-- Additional radios omitted for brevity --> </m3e-radio-group>
(FACE), contributing its state to the form data. The attribute defines the key under which the radio's value is submitted in a form. When checked and not disabled, (defaults to ) is submitted.
When using a radio group, the group's is propagated to all nested radios.
The following example illustrates disabling individual radios in a group.
<label for="radio-group">Select an option</label> <m3e-radio-group id="radio-group"> <label><m3e-radio disabled value="1"></m3e-radio> Option 1</label> <!-- Additional radios omitted for brevity --> </m3e-radio-group>
The next example illustrates disabling an entire group.
<label for="radio-group">Select an option</label> <m3e-radio-group id="radio-group" disabled> <label><m3e-radio value="1"></m3e-radio> Option 1</label> <!-- Additional radios omitted for brevity --> </m3e-radio-group>
Use the attribute of a radio group to require an option to be checked. Associated labels are automatically styled to show the validity state.
In the example below, press TAB to enter the radio group, then TAB again to exit without making a selection to see how validation is trigger. Selecting a radio will revalidate.
<label for="radio-group">Select an option</label> <m3e-radio-group id="radio-group" required> <label><m3e-radio value="1"></m3e-radio> Option 1</label> <!-- Additional radios omitted for brevity --> </m3e-radio-group>
Radios are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Radios support negative density scales down to -3, with each step reducing the visual height by one density unit. Regardless of visual size, radios maintain a 48x48dp minimum touch target to ensure reliable accessibility.
The following illustrates applying density to a radio. Interact with each radio to see how density impacts its state layer.
<label> <m3e-radio class="density-3"></m3e-radio> Density -3 </label> <!-- Additional radios omitted for brevity -->
Each radio group is assigned to signal a related set of options, while individual radios receive to indicate their participation in the group. This ensures screen readers can convey the correct context and selection state to users.
The attribute reflects whether a radio is currently selected within its group. When a radio is checked, is exposed to assistive technologies; otherwise, it is .
When disabled using the attribute, is used to convey to assistive technologies that the radio is disabled. When disabled at runtime, it does
When a radio group is disabled, is not applied to the group. Instead, the disabled state is reflected by applying to each nested radio, ensuring assistive technologies correctly interpret the non-interactive status of individual options.
When a radio group is marked as required using the attribute, is applied to the group. This communicates to assistive technologies that a selection is mandatory. The attribute is not propagated to individual radios, as the requirement applies to the group as a whole, not each option.
Always provide an accessible label via or for radios and radio groups without descriptive text content
The and components provide a Material 3 expressive search experience. The search bar offers a prominent entry point for text input, while the search view presents suggestions, history, and results in contained, docked, or full screen configurations.
Use the as the entry point for search. It provides the interactive affordance that activates search, but does not display results. To present suggestions, history, or results, use , which provides a complete search experience.
— Content positioned at the start of the bar, such as an icon or navigation affordance.
— The element used to capture search text.
— Content aligned to the end of the bar, such as an avatar or overflow menu.
Use the attribute to control whether the bar displays a clear button. When activated, this button clears the input and emits a event.
<m3e-search-bar clearable> <m3e-icon name="search" slot="leading"></m3e-icon> <input slot="input" placeholder="Search..." /> </m3e-search-bar>
Use the to display search suggestions and results. Unlike , which only provides the entry point, the search view presents the results of a search.
— When the view is closed, renders content positioned before the input.
— When the view is closed, renders content positioned after the input.
— When the view is open, renders content positioned before the input.
— When the view is open, renders content after the input.
— Renders the element used to capture search text.
— When the view is open, renders the results content of the view.
Use the attribute to control how results are presented. Supported values include , (default), and , which selects the appropriate mode based on the current viewport breakpoint.
The following example illustrates the default mode. In this mode, the search view appears as a surface anchored to the search bar, allowing users to explore results while maintaining context within the surrounding layout.
<m3e-search-view> <input slot="input" placeholder="Search..." /> <m3e-list> <m3e-list-item>Result One</m3e-list-item> <m3e-list-item>Result Two</m3e-list-item> <m3e-list-item>Result Three</m3e-list-item> </m3e-list> </m3e-search-view>
The next example illustrates the mode. In this mode, the search view occupies the entire viewport, creating an immersive, focused search that temporarily replaces the surrounding UI.
<m3e-search-view mode="fullscreen"> <input slot="input" placeholder="Search..." /> <!-- Results omitted for brevity --> </m3e-search-view>
Use the attribute to a persistent, filled search container when open.
The following example illustrates use of in the mode.
<m3e-search-view mode="docked" contained> <input slot="input" placeholder="Search..." /> <!-- Results omitted for brevity --> </m3e-search-view>
The next example illustrates use of in the mode.
<m3e-search-view mode="fullscreen" contained> <input slot="input" placeholder="Search..." /> <!-- Results omitted for brevity --> </m3e-search-view>
The emits a event that can be used to load results. The event fires under the following conditions:
The receives focus. This can be used to load initial suggestions.
The value changes. This can be used to fetch results that match the current term using the event's property.
In addition, a event is emitted when the user clicks the clear button. This action also triggers a event first, followed by , reflecting the component's input → query → clear event sequence.
While its typical to show a search icon, you can use the attribute to hide it when a search view is closed.
<m3e-search-view mode="docked" hide-search-icon> <input slot="input" placeholder="Search..." /> <!-- Results omitted for brevity --> </m3e-search-view>
The search bar automatically normalizes its slotted to ensure correct search semantics across browsers and assistive technologies. It applies , , and when not already specified. Additionally, is applied to inform assistive technologies that activating the input will open a dialog, allowing screen readers to anticipate the resulting change in context.
When open, the overlay region of the search view is given ARIA indicating that it is a dedicated search surface. The dialog is labeled by its header via , allowing assistive technologies to announce the search context clearly. is used to indicate only the content in the dialog can be interacted with.
The search view does not automatically announce dynamic updates. Instead, you are responsible for sending short, textual updates to a dedicated element when search results change. This ensures assistive technologies receive meaningful feedback without re-announcing the full list of results.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js", "@m3e/web/core/anchoring": "/node_modules/@m3e/web/dist/core-anchoring.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js" "@m3e/web/core/layout": "/node_modules/@m3e/web/dist/core-layout.js" "@m3e/web/icon-button": "/node_modules/@m3e/web/dist/icon-button.js" } } </script>
The component allows users to select one or more options from a horizontal group.
A segmented button is defined using the component, which serves as a semantic container for a set of interactive segments. Each segment is represented by a component, allowing users to select from multiple related options within a unified control.
<m3e-segmented-button> <m3e-button-segment checked>8 oz</m3e-button-segment> <!-- Additional segments omitted for brevity --> </m3e-segmented-button>
Use the slot of the to present an icon before a segment's label.
When selected, a segments's icon is replaced with a check icon.
<m3e-segmented-button> <m3e-button-segment> <m3e-icon slot="icon" name="palette"></m3e-icon>Design </m3e-button-segment> <!-- Additional segments omitted for brevity --> </m3e-segmented-button>
Segmented buttons support both single-select and multi-select modes. Use the attribute of to allow multiple segments to be selected simultaneously.
<m3e-segmented-button multi> <m3e-button-segment checked>8 oz</m3e-button-segment> <m3e-button-segment checked>12 oz</m3e-button-segment> <!-- Additional segments omitted for brevity --> </m3e-segmented-button>
<m3e-segmented-button> <m3e-button-segment disabled> <m3e-icon slot="icon" name="palette"></m3e-icon>Design </m3e-button-segment> <!-- Additional segments omitted for brevity --> </m3e-segmented-button>
You can also use the attribute of to disable the button in its entirety.
<m3e-segmented-button disabled> <m3e-button-segment> <m3e-icon slot="icon" name="palette"></m3e-icon>Design </m3e-button-segment> <!-- Additional segments omitted for brevity --> </m3e-segmented-button>
Use the attribute of to hide selection indicators. If a segment has an icon via use of the slot, the icon is retained upon selection.
<m3e-segmented-button hide-selection-indicator> <!-- Segments omitted for brevity --> </m3e-segmented-button>
Segmented buttons can participate in form submission as a
(FACE), contributing its state to the form data. The attribute defines the key under which the component's value is submitted in a form. The button's value is determined by selected segments. You can use the of a to control a segments's value. When multiple segments are selected, form data will include multiple entries keyed by the same .
The component honors the state (on the segmented button itself) as a
Segmented buttons are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Segmented buttons support negative density scales down to -3, with each step reducing the visual height by one density unit. Regardless of visual size, button segments maintain a 48x48dp minimum touch target to ensure reliable accessibility.
The following illustrates applying density to a segmented button.
<m3e-segmented-button class="density-3"> <!-- Button segments omitted for brevity --> </m3e-segmented-button> <!-- Additional segmented buttons omitted for brevity -->
Segmented buttons are given the ARIA and segments are given for single-select buttons. For multi-select, buttons are given and segments are given . When selected, the segments's selection state is reflected using for single-select buttons and for multi-select buttons.
For disabled single-select segmented buttons, is not applied to the button. Instead, the disabled state is reflected by applying to each nested segment, ensuring assistive technologies correctly interpret the non-interactive status of individual options. For disabled multi-select segmented buttons, is applied to the button as well as individual segments. When a segmented button is disabled at runtime, it does
Always provide an accessible label via or for each segmented button.
The component provides a form control for selecting a value from a set of predefined options.
To define selectable options within a , include one or more elements as its children. Each may specify a attribute, which determines the programmatic value submitted when the option is selected. The textual content of the element represents the label presented to the user in the dropdown interface.
<m3e-form-field> <label slot="label" for="select">Favorite fruit</label> <m3e-select id="select"> <m3e-option>Apples</m3e-option> <m3e-option>Oranges</m3e-option> <m3e-option>Bananas</m3e-option> <m3e-option>Grapes</m3e-option> </m3e-select> </m3e-form-field>
To define an empty option, include a without a value or content. Alternatively, set the attribute to and include descriptive content such as "None". This signals that no meaningful value has been selected. Empty options are commonly used to represent a placeholder or default state prior to user interaction.
<m3e-form-field> <label slot="label" for="select">Favorite fruit</label> <m3e-select id="select"> <m3e-option value="">None</m3e-option> <m3e-option>Apples</m3e-option> <m3e-option>Oranges</m3e-option> <m3e-option>Bananas</m3e-option> <m3e-option>Grapes</m3e-option> </m3e-select> </m3e-form-field>
The component can be used to group common options under a subheading. The name of the group can be set using the slot.
Nested groups are not supported—only a single level of grouping is permitted.
<m3e-form-field> <label slot="label" for="select">Pokemon</label> <m3e-select id="select"> <m3e-optgroup> <span slot="label">Grass</span> <m3e-option>Bulbasaur</m3e-option> <m3e-option>Oddish</m3e-option> <m3e-option>Bellsprout</m3e-option> </m3e-optgroup> <!-- Additional option groups omitted for brevity --> </m3e-select> </m3e-form-field>
Selects support both single-select and multi-select modes. Use the attribute to allow multiple options to be selected simultaneously.
Use the attribute to control the selected state of a . When clicked, a cancellable event is emitted by the followed by an and event. To cancel selection change, call during .
<m3e-form-field> <label slot="label" for="select">Toppings</label> <m3e-select id="select" multi> <m3e-option selected>Extra cheese</m3e-option> <m3e-option selected>Mushroom</m3e-option> <!-- Additional option groups omitted for brevity --> </m3e-select> </m3e-form-field>
You can disable the entire select component or individual options by applying the attribute to or , respectively.
<m3e-form-field> <label slot="label" for="select">Favorite fruit</label> <m3e-select id="select" disabled> <m3e-option selected>Apples</m3e-option> <!-- Additional option groups omitted for brevity --> </m3e-select> </m3e-form-field>
<m3e-form-field> <label slot="label" for="select">Favorite fruit</label> <m3e-select id="select"> <m3e-option disabled>Apples</m3e-option> <!-- Additional option groups omitted for brevity --> </m3e-select> </m3e-form-field>
Apply the attribute to enforce selection. This ensures that users must choose at least one option before the field is considered valid. For multi-select configurations, at least one selected option satisfies the requirement.
<m3e-form-field> <label slot="label" for="select">Favorite fruit</label> <m3e-select id="select" required> <m3e-option value="">None</m3e-option> <m3e-option>Apples</m3e-option> <!-- Additional option groups omitted for brevity --> </m3e-select> </m3e-form-field>
Form fields and select panels are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Both support negative density scales down to -3, with each step reducing the visual height by one density unit.
<m3e-form-field class="density-3"> <label slot="label" for="select">Density -3</label> <m3e-select id="select" panel-class="density-3"> <!-- Options omitted for brevity --> </m3e-select> </m3e-form-field> <!-- Additional fields omitted for brevity -->
The component follows the ARIA interaction pattern. Selects are given the ARIA , while the dropdown panel applies to convey its structure to assistive technologies.
Option groups are given ARIA , indicating that the grouped options form a semantically related set within the listbox. This role helps assistive technologies convey structural relationships between options, enhancing navigability and contextual understanding for screen reader users.
When disabled using the attribute, is used to convey to assistive technologies that an option or select is disabled. When a is disabled at runtime, it does
The and attributes are applied to the element to establish an explicit relationship with the listbox popup. Both attributes reference the of the listbox container, ensuring assistive technologies recognize the connection between the trigger and the controlled content. While indicates that the select governs the visibility and behavior of the listbox, asserts DOM ownership when the listbox is rendered outside the select's subtree.
The component allows you to use abstract shapes thoughtfully to add emphasis and decorative flair, including built-in shape morphing.
The allows you to easily use shapes from the
It supports 31 shapes, specified using the attribute: , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , , and .
<m3e-shape name="12-sided-cookie"></m3e-shape> <m3e-shape name="9-sided-cookie"></m3e-shape> <m3e-shape name="8-leaf-clover"></m3e-shape> <m3e-shape name="7-sided-cookie"></m3e-shape> <m3e-shape name="6-sided-cookie"></m3e-shape> <m3e-shape name="4-sided-cookie"></m3e-shape> <m3e-shape name="4-leaf-clover"></m3e-shape> <br /> <m3e-shape name="arch"></m3e-shape> <m3e-shape name="arrow"></m3e-shape> <m3e-shape name="boom"></m3e-shape> <m3e-shape name="bun"></m3e-shape> <m3e-shape name="diamond"></m3e-shape> <m3e-shape name="fan"></m3e-shape> <m3e-shape name="flower"></m3e-shape> <br /> <m3e-shape name="gem"></m3e-shape> <m3e-shape name="ghost-ish"></m3e-shape> <m3e-shape name="heart"></m3e-shape> <m3e-shape name="hexagon"></m3e-shape> <m3e-shape name="pentagon"></m3e-shape> <m3e-shape name="pill"></m3e-shape> <m3e-shape name="pixel-circle"></m3e-shape> <br /> <m3e-shape name="pixel-triangle"></m3e-shape> <m3e-shape name="puffy"></m3e-shape> <m3e-shape name="puffy-diamond"></m3e-shape> <m3e-shape name="semicircle"></m3e-shape> <m3e-shape name="slanted"></m3e-shape> <m3e-shape name="soft-boom"></m3e-shape> <m3e-shape name="soft-burst"></m3e-shape> <br /> <m3e-shape name="square"></m3e-shape> <m3e-shape name="circle"></m3e-shape> <m3e-shape name="triangle"></m3e-shape>
Nest images or videos inside a to clip them using the shape.
<m3e-shape name="sunny"> <img src="https://www.shutterstock.com/shutterstock/videos/1006393/thumb/1.jpg?ip=x480" /> </m3e-shape> <m3e-shape name="sunny"> <video autoplay loop poster="https://www.shutterstock.com/shutterstock/videos/1006393/thumb/1.jpg?ip=x480" preload="auto" > <source src="https://www.shutterstock.com/shutterstock/videos/1006393/preview/stock-footage-business-people-working-in-office.webm" type="video/webm" /> <source src="https://www.shutterstock.com/shutterstock/videos/1006393/preview/stock-footage-business-people-working-in-office.mp4" type="video/mp4" /> </video> </m3e-shape>
All shape paths are normalized to the same number of points allowing shapes to be morphed using CSS transitions.
By default, shapes are not given an ARIA role, indicating that they are treated as neutral, non-semantic elements in the accessibility tree unless explicitly referenced or annotated.
The component provides a loading placeholder surface with flexible shape variants and motion-based animations that communicate loading state while preserving layout stability. It mimics the layout of content while it's still loading, ensuring a smooth user experience during data fetching or rendering delays.
Use the component to generate loading placeholders for any top-level slotted elements. The component creates an overlay shape for each node, matching its geometry while preserving the layout of the underlying content.
<m3e-skeleton> <m3e-card> <m3e-heading slot="header" variant="display" size="small">Card Header</m3e-heading> <div slot="content">Card Content</div> <div slot="actions"> <m3e-button variant="filled">Action</m3e-button> </div> <div slot="footer">Card Footer</div> </m3e-card> </m3e-skeleton>
Use the attribute to define the geometry applied to all top level slotted content. Each node receives a corresponding skeleton overlay that reflects the selected shape. The option produces a circle based on the element size. The option applies the standard radius token used by most surfaces and controls. The option renders with no radius for a sharp edge appearance. The option reads the actual border radius of the slotted element and mirrors it exactly, ensuring the skeleton aligns with cards, buttons, images, or any custom component without additional configuration.
<m3e-skeleton shape="circular"> <div style="width: 100px; height: 100px"></div> </m3e-skeleton> <br /> <m3e-skeleton shape="rounded"> <div style="width: 100px; height: 100px"></div> </m3e-skeleton> <br /> <m3e-skeleton shape="square"> <div style="width: 100px; height: 100px"></div> </m3e-skeleton> <br /> <m3e-skeleton shape="auto"> <div style="width: 100px; height: 100px; border-radius: 16px"></div> </m3e-skeleton>
Use the attribute to control how the skeleton communicates loading progress. When enabled, the component applies motion across all top level slotted content to indicate that data is being fetched. The option renders a smooth directional sweep that suggests ongoing work. The option applies a rhythmic fade in and fade out effect that draws attention without strong directional movement.
<m3e-skeleton shape="circular" animation="pulse"> <div style="width: 100px; height: 100px"></div> </m3e-skeleton> <!-- Additional skeletons omitted for brevity -->
The option disables motion and displays a static placeholder for environments that prefer reduced movement or for layouts that require a quiet loading state.
<m3e-skeleton shape="circular" animation="none"> <div style="width: 100px; height: 100px"></div> </m3e-skeleton> <!-- Additional skeletons omitted for brevity -->
A skeleton's color uses the lowest perceptual contrast that remains reliably visible, keeping the placeholder subtle and non-intrusive.
Skeletons are purely visual placeholders and should not expose any semantic role or announce themselves to assistive technologies. The element remains neutral and should have no role, no aria-label, no aria-live, and no aria-busy. Loading state should be communicated by applying to a parenting container, ensuring screen readers understand that content is loading without being interrupted by decorative placeholders.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/anchoring": "/node_modules/@m3e/web/dist/core-anchoring.js" } } </script>
The component presents directional pagination controls for navigating overflowing content.
To use the , simply nest content inside it. When content overflows, directional pagination buttons will appear allowing the user to click to scroll its content.
<m3e-slide-group> <div>Item 1</div> <!-- Additional items omitted for brevity --> </m3e-slide-group>
By default, scroll groups are oriented horizontally. Use the attribute to create a vertically oriented scroll group.
<m3e-slide-group vertical> <div>Item 1</div> <!-- Additional items omitted for brevity --> </m3e-slide-group>
Scroll groups are not given an ARIA role, indicating that they are treated as neutral, non-semantic elements in the accessibility tree unless explicitly referenced or annotated.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js", "@m3e/web/icon-button": "/node_modules/@m3e/web/dist/icon-button.js" } } </script>
The component enables users to select a numeric value from a continuous or discrete range.
The allows for the selection of a value from a range similar to . They require a , which is responsible for rendering the interactive handle, managing pointer and keyboard input, and conveying current value semantics
By default the minimum value of the slider is , the maximum value is , and the thumb moves in increments of . These values can be changed by setting the , , and attributes respectively.
The thumb value is set to the minimum value unless otherwise specified using the attribute of the . When changed through user interaction, a event is emitted followed by an and event.
A can be converted into a range slider by specifying two thumbs.
Negative value ranges are supported by setting the attribute to a negative value.
By default, sliders are oriented horizontally. Use the attribute to change the orientation to (default) or .
There are five size variants which can be used to add or remove emphasis: (default), , , , and . Use the attribute to control a slider's size.
<m3e-slider size="extra-small"> <m3e-slider-thumb value="20"></m3e-slider-thumb> </m3e-slider> <!-- Additional sizes omitted for brevity -->
Use the attribute of a to control whether to show thumb labels. By default, the value of a thumb is used as the label text. You can control label formatting by setting the property to a function that returns a label given a value.
By default, tick marks are not presented. Use the attribute to show them. You must also specify a larger than for tick marks to show.
Sliders can be disabled using the attribute. If labelled, thumb labels are shown on hover.
While uncommon, individual thumbs can also be disabled while allowing another thumb to be enabled.
<m3e-slider labelled> <m3e-slider-thumb value="20"></m3e-slider-thumb> <m3e-slider-thumb disabled value="80"></m3e-slider-thumb> </m3e-slider>
Sliders are not given an ARIA role, indicating that they are treated as neutral, non-semantic elements in the accessibility tree unless explicitly referenced or annotated.
Thumbs are given ARIA indicating that each functions as an independent value selector within the range, conveying its current position via , , and attributes.
When disabled, is used to convey to assistive technologies that a thumbs are present but intentionally non-interactive.
Always provide an accessible label via or for each thumb.
The module provides the global service on ( ) used to present short updates about application processes at the bottom of the screen from anywhere in an application.
Use the method of the service to open a snackbar.
When opening a snackbar message, you can pass a object that includes the following properties.
— The length of time, in milliseconds, to wait before automatically dismissing the snackbar.
— The accessible label given to the close button used to dismiss the snackbar.
— The callback function invoked when the action is clicked.
Snackbar messages are given ARIA and , indicating that they are polite live regions intended to be announced by assistive technologies without requiring focus.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/button": "/node_modules/@m3e/web/dist/button.js", "@m3e/web/icon-button": "/node_modules/@m3e/web/dist/icon-button.js" } } </script>
The component presents a primary action alongside a menu of related actions, uniting two buttons in a single expressive surface.
The exposes two action slots: for the primary intent, and for invoking a menu of secondary options. The leading button executes the main action directly, while the trailing button reveals contextual alternatives via an attached menu.
<m3e-split-button> <m3e-button slot="leading-button"> <m3e-icon slot="icon" name="edit"></m3e-icon>Edit </m3e-button> <m3e-icon-button slot="trailing-button"> <m3e-icon name="keyboard_arrow_down"></m3e-icon> <m3e-menu-trigger for="menu"></m3e-menu-trigger> </m3e-icon-button> </m3e-split-button> <m3e-menu id="menu" position-x="before"> <m3e-menu-item>Rename</m3e-menu-item> <m3e-menu-item>Copy</m3e-menu-item> <m3e-menu-item>Delete</m3e-menu-item> </m3e-menu>
There are four split button appearance variants: (default), , , and . Use the attribute to change the appearance.
<m3e-split-button variant="filled"> <m3e-button slot="leading-button"> <m3e-icon slot="icon" name="edit"></m3e-icon>Edit </m3e-button> <m3e-icon-button slot="trailing-button"> <m3e-icon name="keyboard_arrow_down"></m3e-icon> <m3e-menu-trigger for="menu"></m3e-menu-trigger> </m3e-icon-button> </m3e-split-button> <!-- Additional variants omitted for brevity --> <!-- Menu omitted for brevity -->
There are five size variants which can be used to add or remove emphasis: , (default), , , and . Use the attribute to control the split button's size.
<m3e-split-button size="extra-small"> <m3e-button slot="leading-button"> <m3e-icon slot="icon" name="edit"></m3e-icon>Edit </m3e-button> <m3e-icon-button slot="trailing-button"> <m3e-icon name="keyboard_arrow_down"></m3e-icon> <m3e-menu-trigger for="menu"></m3e-menu-trigger> </m3e-icon-button> </m3e-split-button> <!-- Additional sizes omitted for brevity --> <!-- Menu omitted for brevity -->
Split buttons are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Buttons and menus support negative density scales down to -3, with each step reducing the visual height by one density unit. Regardless of visual size, buttons maintain a 48x48dp minimum touch target to ensure reliable accessibility.
The following illustrates applying -3 density to the buttons and menu of a split button.
<m3e-split-button size="extra-small" class="density-3"> <m3e-button slot="leading-button"> <m3e-icon slot="icon" name="edit"></m3e-icon>Edit </m3e-button> <m3e-icon-button slot="trailing-button"> <m3e-icon name="keyboard_arrow_down"></m3e-icon> <m3e-menu-trigger for="menu"></m3e-menu-trigger> </m3e-icon-button> </m3e-split-button> <m3e-menu id="menu" position-x="before" class="density-3"> <!-- Menu items omitted for brevity --> </m3e-menu>
By default, split buttons are given the ARIA role , indicating a set of related user interface elements that are grouped together.
Always provide an accessible label via or for each split button.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js", "@m3e/web/core/anchoring": "/node_modules/@m3e/web/dist/core-anchoring.js", "@m3e/web/button": "/node_modules/@m3e/web/button.js", "@m3e/web/icon-button": "/node_modules/@m3e/web/icon-button.js", "@m3e/web/button-group": "/node_modules/@m3e/web/button-group.js" } } </script>
The component delivers a Material 3-inspired split view with a movable drag handle, enabling responsive layout composition and precise pane resizing. It supports keyboard interaction, adaptive orientation, and optional detent snapping for consistent, accessible content distribution.
The exposes two slots: , which renders content on the logical start side of the layout, and , which renders content on the logical end side. The attribute controls the percentage of the available space occupied by the start pane, with the end pane automatically filling the remainder. By default, is 50%. When a user interaction causes to change, a cancellable event is emitted, followed by an and event. This allows you to react to live resizing as well as commit final adjustments when the user finishes interacting with the drag handle.
The supports three orientations: (default), , and . Using , the orientation is responsive and determined by viewport size. Use the attribute to change the orientation.
<m3e-split-pane value="50" orientation="vertical"> <m3e-card slot="start"></m3e-card> <m3e-card slot="end"></m3e-card> </m3e-split-pane>
Use the and attributes to control the minimum and maximum percentage the start pane can occupy.
<m3e-split-pane value="50" min="25" max="75"> <m3e-card slot="start"></m3e-card> <m3e-card slot="end"></m3e-card> </m3e-split-pane>
Use the attribute to control how much the split pane's changes with each keyboard resize action, such as pressing the arrow keys.
<m3e-split-pane value="50" step="10"> <m3e-card slot="start"></m3e-card> <m3e-card slot="end"></m3e-card> </m3e-split-pane>
Use the attribute to define the positions a split pane can snap to during resizing. A split pane may have no detents, a single detent, or multiple detents. Detents influence how the pane resizes, how it responds to drag gestures, and how the drag handle behaves during keyboard interaction. When detents are present, the drag handle snaps to the nearest detent as the user moves it and is ignored; when no detents are defined, the split pane resizes freely without snapping.
Double-clicking the drag handle, or pressing or when it is focused, will cycle to the next detent. Use the attribute to allow cycling to wrap from the last detent back to the first.
<m3e-split-pane value="50" detents="0 25 50 75 100" wrap-detents> <m3e-card slot="start"></m3e-card> <m3e-card slot="end"></m3e-card> </m3e-split-pane>
<m3e-split-pane value="50" disabled> <m3e-card slot="start"></m3e-card> <m3e-card slot="end"></m3e-card> </m3e-split-pane>
You can conditionally render the start or end pane either by omitting content from the corresponding slot or by applying the attribute to slotted elements. When all assigned elements in a slot are hidden, the split pane treats that side as empty and updates the layout so the visible pane fully occupies the available space without modifying the current .
<m3e-split-pane value="50"> <m3e-card slot="start"></m3e-card> <m3e-card slot="end" hidden></m3e-card> </m3e-split-pane>
Split panes can be nested to create complex layouts, allowing you to build multi-pane workspaces by combining multiple binary splits.
<m3e-split-pane value="25" class="complex"> <m3e-card slot="start"></m3e-card> <m3e-split-pane slot="end" value="50" orientation="vertical"> <m3e-card slot="start"></m3e-card> <m3e-card slot="end"></m3e-card> </m3e-split-pane> </m3e-split-pane>
(FACE), contributing its state to the form data. The attribute defines the key under which the component's value is submitted in a form. When not disabled, the current is submitted.
Split panes use the ARIA , indicating that the drag handle is an interactive control used to adjust the relative size of two regions.
The drag handle uses to reference the pane it resizes. The start pane is wrapped in an internal container element with a stable ID, and the drag handle's attribute points to that container. This allows assistive technologies to understand which region is affected when the user adjusts the split pane.
The split pane supports a attribute, which maps directly to the drag handle's . This allows you to provide a clear, descriptive name for screen reader users, such as “Resize panels” or “Adjust split view”.
The component exposes its orientation, current position, and size constraints through standard ARIA attributes so assistive technologies can accurately convey the layout to users. These attributes allow screen readers to announce how the split pane is arranged, how far the drag handle has been moved, and what the allowable range of movement is.
communicates whether the panes are arranged horizontally or vertically, helping users understand the spatial direction in which the drag handle can be moved.
reports the drag handle's current position as a percentage, allowing screen readers to announce the exact size of the start pane.
and define the minimum and maximum allowed sizes, ensuring users know the full range of movement and whether further adjustment is possible.
provides a human-readable description of the current size (for example, “Left pane 20%, right pane 80%” or “Top pane collapsed”), improving clarity for screen reader users. You may customize this text by supplying a function, which allows full control over how the size is described. This is useful for localization, or presenting alternative formats.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js", "@m3e/web/core/layout": "/node_modules/@m3e/web/dist/core-layout.js" } } </script>
The component provides a wizard-like workflow by dividing content into logical steps.
Use , , and to construct a wizard-like workflow. Both and must be direct children of . To associate a step with its corresponding panel, set the attribute on to match the ID of the target .
To select a step, apply the attribute to a single . When a step is activated, a cancellable event is emitted followed by an and event. To cancel selection change, call during . Only one step may be selected at a time.
<m3e-stepper> <m3e-step for="step1">Fill out your name</m3e-step> <m3e-step for="step2">Fill out your address</m3e-step> <m3e-step for="step3">Done</m3e-step> <m3e-step-panel id="step1"> <form> <m3e-form-field> <label slot="label" for="name">Name</label> <input name="name" id="name" required /> </m3e-form-field> </form> <div slot="actions"> <m3e-button><m3e-stepper-next>Next</m3e-stepper-next></m3e-button> </div> </m3e-step-panel> <m3e-step-panel id="step2"> <form> <m3e-form-field> <label slot="label" for="address">Address</label> <input name="address" id="address" required /> </m3e-form-field> </form> <div slot="actions"> <m3e-button><m3e-stepper-previous>Back</m3e-stepper-previous></m3e-button> <m3e-button><m3e-stepper-next>Next</m3e-stepper-next></m3e-button> </div> </m3e-step-panel> <m3e-step-panel id="step3"> Done <div slot="actions"> <m3e-button><m3e-stepper-previous>Back</m3e-stepper-previous></m3e-button> <m3e-button><m3e-stepper-reset>Reset</m3e-stepper-reset></m3e-button> </div> </m3e-step-panel> </m3e-stepper>
The supports three orientations: (default), , and . Using , the orientation is responsive and determined by viewport size. Use the attribute to change a stepper's orientation.
<m3e-stepper orientation="vertical"> <!-- Content omitted for brevity --> </m3e-stepper>
When using a horizontal stepper, you can control where the stepper's content is positioned using the attribute. By default, the header is placed content, but you can change it to to placed it under the content.
<m3e-stepper header-position="below"> <!-- Content omitted for brevity --> </m3e-stepper>
When using a horizontal stepper, you can control where the step's label is positioned using the attribute. By default, labels are placed a step's icon, but you can change it to to placed it under the icon.
<m3e-stepper label-position="below"> <!-- Content omitted for brevity --> </m3e-stepper>
Panels provide an slot for placing navigation controls. The , , and components can be used to navigate steps. Using the will reset the stepper and any form data.
The attribute can be set on to create a linear stepper that requires the user to complete previous steps before proceeding to following steps.
Linear steppers require use of a per step whose validity status determines whether a user can move to the next step.
A step is considered complete once the user transitions forward. Navigation back to an earlier step is allowed only when that step is explicitly designated as editable via the attribute.
<m3e-stepper linear> <m3e-step for="step1" editable>Fill out your name</m3e-step> <m3e-step for="step2" editable>Fill out your address</m3e-step> <m3e-step for="step3">Done</m3e-step> <!-- Content omitted for brevity --> </m3e-stepper>
Use the attribute on to indicate that completion is not required. When navigating away from an optional step, its nested is still validated; however, an invalid state will neither block progression nor visually mark the step header as invalid.
For linear steppers, use the attribute on to allow a user to navigate back to a previously completed step.
You can use the and attributes to programmatically control whether a step is completed or invalid.
You can override icons using the , , , and .
You can use the and slots of a to specify hint text or, when invalid, an error message. By default, optional steps have hint text of .
and are composed of the , , and roles. The pattern includes support for keyboard navigation, focus management, and accessible relationships between steps and their associated panels.
The component hosts an internal . It contains each , which is assigned the ARIA . Corresponding elements are given the ARIA . Each step is linked to its associated panel using , and the attribute communicates the selected state of a step to assistive technologies. When a step is disabled, communicates its inactive state.
The component is nn on/off control that can be toggled by clicking.
Use the component to create a switch (also known as a slide toggle). Use the attribute to indicate whether the switch is "on". When clicked, a cancellable event is emitted followed by an and event. To cancel checked state changes, call during .
Associate a label with a switch using the element.
<label><m3e-switch></m3e-switch> Switch 1</label> <m3e-switch id="switch2"></m3e-switch> <label for="switch2"> Switch 2</label>
You can control the icons to present using the attribute. Supports values include (default), , and .
<label><m3e-switch disabled></m3e-switch>Disabled Switch 1</label> <m3e-switch id="chk3" disabled></m3e-switch> <label for="chk3">Disabled Switch 2</label>
Switches are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Switches support negative density scales down to -3, with each step reducing the visual height by one density unit. Regardless of visual size, switches maintain a 48x48dp minimum touch target to ensure reliable accessibility.
The following illustrates applying density to assist switches.
<m3e-switch class="density-3"></m3e-switch> <!-- Additional switches omitted for brevity -->
Switches are given the ARIA aligning with their semantic purpose as binary state toggles. This role communicates to assistive technologies that the component represents a two-state control, similar to a checkbox but with a distinct interaction model.
The component manages and reflects its state via the attribute, where indicates the switch is checked (on), and indicates it is unchecked (off).
When disabled using the attribute, is used to convey to assistive technologies that the switch is disabled. When disabled at runtime, it does
The component provides a structured navigation surface for organizing content into distinct views, where only one view is visible at a time.
Use , , and to construct tabbed views. Both and must be direct children of . To associate a tab with its corresponding panel, set the attribute on to match the ID of the target .
To select a tab, apply the attribute to a single . When a tab is activated, a cancellable event is emitted followed by an and event. To cancel selection change, call during . Only one tab may be selected at a time.
A tab's label is specified by its content. You can use the slot to render an icon before a tab's label.
<m3e-tabs> <m3e-tab selected for="videos"> <m3e-icon slot="icon" name="videocam"></m3e-icon>Video </m3e-tab> <m3e-tab for="photos"> <m3e-icon slot="icon" name="photo"></m3e-icon>Photos </m3e-tab> <m3e-tab for="audio"> <m3e-icon slot="icon" name="music_note"></m3e-icon>Audio </m3e-tab> <m3e-tab-panel id="videos">Videos</m3e-tab-panel> <m3e-tab-panel id="photos">Photos</m3e-tab-panel> <m3e-tab-panel id="audio">Audio</m3e-tab-panel> </m3e-tabs>
Tabs come in the appearance variants: and (default). Use the attribute of to change the appearance.
<m3e-tabs variant="primary"> <!-- Content omitted for brevity --> </m3e-tabs>
Use the attribute to stretch tabs to fill the header.
<m3e-tabs stretch> <!-- Content omitted for brevity --> </m3e-tabs>
A tab's header can be positioned (default) or panels. Use the attribute to change the header position.
<m3e-tabs header-position="after"> <!-- Content omitted for brevity --> </m3e-tabs>
<m3e-tabs> <m3e-tab selected for="videos"> <m3e-icon slot="icon" name="videocam"></m3e-icon>Video </m3e-tab> <m3e-tab disabled for="photos"> <m3e-icon slot="icon" name="photo"></m3e-icon>Photos </m3e-tab> <!-- Additional content omitted for brevity --> </m3e-tabs>
By default, directional pagination controls appear when the number of tabs exceeds the available horizontal space. You can disable this using the attribute. Set this to to disable pagination controls on touch devices.
The following example demonstrates pagination behavior when tabs overflow. To observe it in action, reduce your screen width until directional controls appear.
Tabs are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Tabs support negative density scales down to -3, with each step reducing the visual height by one density unit.
The following illustrates applying -3 density to tabs.
<m3e-tabs class="density-3"> <!-- Tabs omitted for brevity --> </m3e-tabs>
and are composed of the , , and roles. The pattern includes support for keyboard navigation, focus management, and accessible relationships between tabs and their associated panels.
The component hosts an internal nested within the pagination controls. It contains each , which is assigned the ARIA . Corresponding elements are given the ARIA . Each tab is linked to its associated panel using , and the attribute communicates the selected state of a tab to assistive technologies. When a tab is disabled, communicates its inactive state.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js", "@m3e/web/icon-button": "/node_modules/@m3e/web/icon-button.js", "@m3e/web/slide-group": "/node_modules/@m3e/web/slide-group.js" } } </script>
The component automatically adjusts the height of a linked to fit its content, preserving layout integrity and user experience. This non-visual element listens to input changes and applies dynamic resizing, constrained by optional row limits.
Use the component to make a automatically resize to fit its content. Use the attribute to specify the to autosize. The following example illustrates use of the alongside a . See
<m3e-form-field variant="filled"> <label slot="label" for="field">Textarea Autosize</label> <textarea id="field"> <!-- Content omitted for brevity --> </textarea> </m3e-form-field> <m3e-textarea-autosize for="field"></m3e-textarea-autosize>
Use the and attributes to control the minimum and maximum number of rows for a .
<m3e-form-field variant="filled"> <label slot="label" for="field">Textarea Autosize</label> <textarea id="field"> <!-- Content omitted for brevity --> </textarea> </m3e-form-field> <m3e-textarea-autosize for="field" max-rows="5"></m3e-textarea-autosize>
The component is given ARIA indicating the element is presentational and should be ignored by assistive technologies.
The component is a non-visual element used to apply dynamic color, expressive motion, density, and strong focus indicators to nested, theme-aware elements.
Use the attribute to specify a HEX color from which to derive color palettes. This will set CSS custom properties that reflect the standard Material Design 3 color palettes. For example, is set to the primary color.
<m3e-theme color="#4285F4"> <!-- Swatches omitted for brevity --> </m3e-theme>
Themes support three color schemes: , , and (default). Use the attribute to specify the color scheme.
When is used, the theme dynamically responds to system preferences by monitoring the media queries and , selecting the appropriate color scheme based on the user's environment.
<m3e-theme color="#4285F4" scheme="dark"> <!-- Swatches omitted for brevity --> </m3e-theme>
Themes support three contrast levels: (default), , and . Use the attribute to control how strongly tones are separated within the theme.
The following illustrates how colors are adjusted for high contrast.
<m3e-theme color="#4285F4" contrast="high"> <!-- Swatches omitted for brevity --> </m3e-theme>
Use the attribute to control layout compactness across density-aware components within a theme. It adjusts spacing, padding, and sizing to achieve a more compact or more spacious visual rhythm.
Applies globally to all components within the theme
When a theme is initialized with a value, it sets the system token to reflect that value. This token is used by density-aware components to adjust their internal layout metrics.
Compact density reduces vertical padding and component height
Touch targets remain accessible and compliant with minimum size guidelines
The following example demonstrates how responds to theme-level density adjustments. A medium-sized button is shown, which supports a minimum density of . Note that densities and produce identical layout behavior for this size, as values below the supported threshold are clamped to .
<m3e-theme density="-3"> <!-- Buttons omitted for brevity --> </m3e-theme> <!-- Additional densities omitted for brevity -->
Themes support two motion schemes: (default) and . These schemes define how components animate across the system, using spring-based physics to guide spatial and effect transitions. Use the attribute to control the motion scheme.
The following example illustrates how the adapts to the motion scheme. Note the spring effect when clicking the expressive example.
<m3e-theme motion="standard"> <!-- Switch omitted for brevity --> </m3e-theme> <m3e-theme motion="expressive"> <!-- Switch omitted for brevity --> </m3e-theme>
By default, themes are not given an ARIA role, indicating that they are treated as neutral, non-semantic elements in the accessibility tree unless explicitly referenced or annotated.
The component presents a temporary surface for selecting time using dial and keyboard modes. It supports hour and minute selection across 12-hour and 24-hour formats, enforces minimum and maximum constraints, and adapts between docked and modal layouts based on available space.
The supports three orientations: (default), , and . Use the attribute to control how the picker is orientated. When set to , the picker automatically selects the appropriate orientation based on the current screen size.
Typically, should be pared with so that the picker will swap between orientation and variant depending on viewport constraints.
Use the and attributes to disable all times in the picker before or after the respective values.
The package includes the and components, allowing you to use them in custom layouts or independently of .
The attribute can be used on to reference a by ID. When present, the input automatically synchronizes its properties to the referenced dial, keeping both components in sync without requiring manual event wiring.
document.querySelector("#input").addEventListener("change", (e) => { const picker = e.target; document.querySelector("#inputValue").innerText = `hour = ${picker.hour ?? ""}, minute = ${picker.minute ?? ""}`; });
Timepickers are given ARIA to identify the picker surface as a discrete interactive region that opens above the page and requires user attention. When the picker is shown in its modal variant, it also sets to indicate that the rest of the page is inert and that focus is trapped within the dialog until it is dismissed. In docked mode, the picker omits entirely, since the surrounding page remains interactive and the surface behaves like an anchored popover rather than a blocking dialog.
Timepicker toggles add to its parenting element indicating to assistive technologies that activating the control will open a dialog.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js" "@m3e/web/core/anchoring": "/node_modules/@m3e/web/dist/core-anchoring.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js", "@m3e/web/core/layout": "/node_modules/@m3e/web/dist/core-layout.js" "@m3e/web/button": "/node_modules/@m3e/web/dist/button.js", "@m3e/web/icon-button": "/node_modules/@m3e/web/dist/icon-button.js", } } </script>
The component generates a hierarchical table of contents for in-page navigation.
The component automatically generates a table of contents by scanning heading elements ( through , and ) within a specified container. It creates a navigable outline that reflects the document's structure, enabling users to quickly jump between sections.
To activate, set the attribute to the ID of the container whose headings should be indexed. The generated TOC updates reactively as headings are added, removed, or reordered.
Headers having the attribute will be ignored by the TOC generator. This is typically used for page-level headings whose text is mirrored by the TOC's title.
<div id="d1"> <m3e-toc for="d1"> <span slot="overline">Overline</span> <span slot="title">Title</span> </m3e-toc> <div> <h2 id="h1">Heading 1</h2> <h3 id="h1.1">Heading 1.1</h3> <h4 id="h1.1.1">Heading 1.1.1</h4> <h3 id="h2">Heading 1.2</h3> <h2 id="h2.1">Heading 2</h2> </div> </div>
Use the and slots to provide heading content for a TOC. The overline is limited to a single line and the title is limited to two lines. If content overflows, it will be truncated with an ellipsis.
When generating a TOC, you can control the maximum depth of headers that appear in the TOC using the attribute. By default, the maximum depth is 2.
The heading depth is determined by the highest-level heading found within the container. For example, if no is present but an is, the depth starts at level 2.
The component is marked with to signal its purpose as a navigational landmark.
Individual items are organized within an unordered list and given ARIA to convey their interactive nature to assistive technologies. Each item also receives to make it programmatically focusable without being included in the default tab order.
The active item receives to indicate its current state within the document context. This attribute helps assistive technologies convey that the linked section is currently in view, enhancing orientation and navigational clarity for screen reader users.
The component presents contextual actions, navigation, and controls.
There are two toolbar appearance variants: (default) and . Use the attribute to change the appearance.
<m3e-toolbar variant="standard"> <m3e-icon-button> <m3e-icon name="arrow_back"></m3e-icon> </m3e-icon-button> <m3e-icon-button> <m3e-icon name="arrow_forward"></m3e-icon> </m3e-icon-button> <m3e-icon-button width="wide" variant="filled"> <m3e-icon name="add"></m3e-icon> </m3e-icon-button> <m3e-icon-button> <m3e-icon name="picture_in_picture"></m3e-icon> </m3e-icon-button> <m3e-icon-button> <m3e-icon name="more_vert"></m3e-icon> </m3e-icon-button> </m3e-toolbar> <m3e-toolbar variant="vibrant"> <!-- Content omitted for brevity --> </m3e-toolbar>
There are two shape variants: (default) and . Use the attribute to change the appearance.
<m3e-toolbar variant="vibrant" shape="rounded"> <!-- Content omitted for brevity --> </m3e-toolbar>
Use the attribute to elevate a toolbar to a visually prominent surface, distinguishing it from the surrounding layout and signaling its importance or active context.
<m3e-toolbar variant="vibrant" shape="rounded" elevated> <!-- Content omitted for brevity --> </m3e-toolbar>
By default, toolbar are oriented horizontally. Use the attribute to orient the toolbar vertically, stacking its interactive elements from top to bottom rather than left to right. This is useful for side panels, compact layouts, or contexts where vertical alignment enhances clarity or accessibility.
<m3e-toolbar variant="vibrant" shape="rounded" vertical> <!-- Content omitted for brevity --> </m3e-toolbar>
Toolbars are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Toolbars support negative density scales down to -3, with each step reducing the visual height by one density unit.
The following illustrates applying -3 density to a toolbar.
<m3e-toolbar class="density-3"> <!-- Content omitted for brevity --> </m3e-toolbar>
Toolbars are given ARIA indicating that the element serves as a container for a group of interactive controls, typically buttons, that perform actions related to a common context. This role helps assistive technologies understand the grouping and purpose of the controls within, enhancing navigation and discoverability.
Vertically oriented toolbars are given to explicitly communicate their layout direction to assistive technologies, enhancing navigation and spatial understanding for users relying on screen readers or keyboard interaction.
The module provides plain and rich tooltip components for delivering contextual guidance, from simple hover descriptions to multi-line, expressive content anchored to a control.
Use the component to define a plain tooltip, and apply the attribute to associate it with a specific UI element.
<m3e-icon-button id="button"> <m3e-icon name="arrow_back"></m3e-icon> </m3e-icon-button> <m3e-tooltip for="button">Go Back</m3e-tooltip>
By default, tooltips are positioned below the element. You can control a tooltip's position using the attribute. Supported values include , (default), , and . The and positions are BIDI-aware, adapting to left-to-right and right-to-left text directions.
Use the and attributes to control the amount of time, in milliseconds, to show or hide a tooltip. The following snippet illustrates the default delays.
<m3e-icon-button id="button"> <m3e-icon name="arrow_back"></m3e-icon> </m3e-icon-button> <m3e-tooltip for="button" show-delay="0" hide-delay="200">Go Back</m3e-tooltip>
<m3e-icon-button id="button"> <m3e-icon name="arrow_back"></m3e-icon> </m3e-icon-button> <m3e-tooltip for="button" disabled>Go Back</m3e-tooltip>
On touch devices, tooltips use a long-press gesture to show and hide. This can conflict with native browser gestures, such as text selection or drag interactions. To mitigate this, tooltips disables native gestures on the tooltip trigger by default. However, this behavior may not be desirable for certain elements like inputs or draggable components.
Use the attribute to specify how touch interactions are handled:
— Enables touch gestures for all elements, while preserving native browser behaviors on inputs, textareas, and draggable elements.
— Enables touch gestures universally and disables all native browser gestures without exception.
— Disables touch gestures entirely, preventing tooltips from appearing on touch devices.
Use the component to define a rich tooltip, and apply the attribute to associate it with a specific UI element. The slot specifies the subhead content and the default slot specifies supplementary text.
<m3e-icon-button id="button"> <m3e-icon name="settings"></m3e-icon> </m3e-icon-button> <m3e-rich-tooltip for="button"> <span slot="subhead">New settings available</span> Now you can adjust the uploaded image quality, and upgrade your available storage space. </m3e-rich-tooltip>
Including interactive content in a rich tooltip promotes it to dialog-like behavior. The surface becomes dismissible and keyboard-navigable, and must be opened through an explicit user action on the trigger.
You can include actions in a rich tooltip using the slot. In addition, use the component, nested in a clickable element, to dismiss the tooltip. By default, focus is returned to the trigger. Use the attribute of a to disable this behavior.
<m3e-icon-button id="button"> <m3e-icon name="settings"></m3e-icon> </m3e-icon-button> <m3e-rich-tooltip for="button"> <span slot="subhead">New settings available</span> Now you can adjust the uploaded image quality, and upgrade your available storage space. <div slot="actions"> <m3e-button> <m3e-rich-tooltip-action>Learn more</m3e-rich-tooltip-action> </m3e-button> </div> </m3e-rich-tooltip>
By default, rich tooltips are positioned below and after the element. You can control a tooltip's position using the attribute. Supported values include , , , (default), , , and . Positions are BIDI-aware, adapting to left-to-right and right-to-left text directions.
Plain and non-interactive tooltips add an description that provides a reference to a visually hidden element containing the tooltip's message. This allows screen readers to announce the tooltip's contents when the user focuses on the tooltip's trigger. The element referenced by is not the tooltip itself, but an invisible copy of its content that remains present in the DOM.
Interactive rich tooltips add to their trigger to correctly expose a dialog-like popup to assistive technologies. In addition, the tooltip's internal popover is given ARIA and is automatically labelled by tooltip's .
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js", "@m3e/web/core/anchoring": "/node_modules/@m3e/web/dist/core-anchoring.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js", "@m3e/web/core/platform": "/node_modules/@m3e/web/dist/core-platform.js" } } </script>
The component presents hierarchical data in a structure that users can navigate, with nested levels that open and collapse as needed.
A basic tree consists of an component containing one or more . Items can be nested to create deeper levels in the hierarchy. Each item supplies its text through the slot. Use the attribute to control whether an item with nested children is expanded.
<m3e-tree> <m3e-tree-item open> <span slot="label">Getting Started</span> <m3e-tree-item> <span slot="label">Overview</span> </m3e-tree-item> <m3e-tree-item> <span slot="label">Installation</span> </m3e-tree-item> </m3e-tree-item> <!-- Additional items omitted for brevity --> </m3e-tree>
Use the attribute to mark an item as selected. The event fires when the user changes selection, and the property provides access to the currently selected item(s).
<m3e-tree> <m3e-tree-item open> <span slot="label">Getting Started</span> <m3e-tree-item selected> <span slot="label">Overview</span> </m3e-tree-item> <m3e-tree-item> <span slot="label">Installation</span> </m3e-tree-item> </m3e-tree-item> <!-- Additional items omitted for brevity --> </m3e-tree>
Trees support both single-select and multi-select modes. Use the attribute of to allow multiple item to be selected simultaneously.
<m3e-tree multi> <m3e-tree-item> <span slot="label">Fruits</span> <m3e-tree-item> <span slot="label">Apples</span> </m3e-tree-item> <m3e-tree-item> <span slot="label">Oranges</span> </m3e-tree-item> <m3e-tree-item> <span slot="label">Bananas</span> </m3e-tree-item> </m3e-tree-item> <!-- Additional items omitted for brevity --> </m3e-tree>
Use the attribute in conjunction with to enable cascade item selection.
<m3e-tree multi cascade> <m3e-tree-item> <span slot="label">Fruits</span> <m3e-tree-item> <span slot="label">Apples</span> </m3e-tree-item> <m3e-tree-item> <span slot="label">Oranges</span> </m3e-tree-item> <m3e-tree-item> <span slot="label">Bananas</span> </m3e-tree-item> </m3e-tree-item> <!-- Additional items omitted for brevity --> </m3e-tree>
Use the and slots to present an icon for an item.
<m3e-tree> <m3e-tree-item open> <m3e-icon slot="icon" name="rocket_launch"></m3e-icon> <span slot="label">Getting Started</span> <m3e-tree-item> <m3e-icon slot="icon" name="near_me"></m3e-icon> <span slot="label">Overview</span> </m3e-tree-item> <m3e-tree-item> <m3e-icon slot="icon" name="package_2"></m3e-icon> <span slot="label">Installation</span> </m3e-tree-item> </m3e-tree-item> </m3e-tree>
Use the and to change the icons used to expand and collapse an item.
<m3e-tree> <m3e-tree-item open> <m3e-icon slot="toggle-icon" name="add_box"></m3e-icon> <m3e-icon slot="open-toggle-icon" name="indeterminate_check_box"></m3e-icon> <span slot="label">Getting Started</span> <!-- Items omitted for brevity --> </m3e-tree-item> </m3e-tree>
Trees are density-aware and adjust their vertical spacing based on the and custom CSS properties. The default density size is 4dp. Trees support negative density scales down to -3, with each step reducing the visual height by one density unit.
The following illustrates applying -3 density to a tree.
<m3e-tree class="density-3"> <!-- Tree items omitted for brevity --> </m3e-tree>
Trees are given ARIA , and items are assigned , indicating a structured, hierarchical navigation model that supports keyboard interaction, semantic grouping, and accessible labeling.
Expandable items use to indicate visibility of nested content. When selected, the item's selection state is reflected using for single-select trees and for multi-select trees.
Icons are given ensure they're treated as purely decorative and excluded from the accessibility tree.
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js" } } </script>
M3E does not require a dedicated Angular bindings package. Angular provides first-class support for Web Components, allowing all M3E components to be used directly in Angular templates without additional wrappers.
However, Angular requires the to recognize custom elements. Add it to your module definition:
import { NgModule, CUSTOM_ELEMENTS_SCHEMA } from "@angular/core"; @NgModule({ declarations: [...], imports: [...], schemas: [CUSTOM_ELEMENTS_SCHEMA], }) export class AppModule {}
package provides idiomatic React bindings for all M3E Web Components. Each component is exposed as its own React module, offering typed props, event mappings, and seamless integration with the React rendering model. However, the React bindings are
After installation, import the React module for any component you want to use. Each module wraps the corresponding module and exposes a familiar React component API.
React modules automatically register their underlying Web Component, so no additional side-effect imports from are required.
Alternatively, import the module to load every React binding at once.
This option is convenient when you want full access to the M3E component suite without managing individual modules.
Each React module mirrors the structure of its Web Component counterpart, ensuring a consistent mental model across both ecosystems. Use individual modules for tree-shaking and minimal bundles, or the aggregated module for rapid prototyping and full-suite development.
M3E does not require a dedicated Vue bindings package. Vue provides first-class support for Web Components, allowing all M3E components to be used directly in Vue templates without additional wrappers.
M3E supports the latest stable releases of all major evergreen browsers. Older versions may work but are not actively tested.
The M3E Web Component library is distributed as a single package under the
After installing the package, each component can be imported individually through its own module.
Alternatively, import the module to load every component at once.
This option is convenient when you want full access to the M3E ecosystem without managing individual modules.
and documents their native module support. To use a module directly in a browser without a bundler, use a module script similar to the following.
to include dependencies. The following is an example import map that imports all entry points for .
<script type="importmap"> { "imports": { "tslib": "https://cdn.jsdelivr.net/npm/tslib@2.8.1/+esm", "lit": "https://cdn.jsdelivr.net/npm/lit@3.3.0/+esm", "lit/": "https://cdn.jsdelivr.net/npm/lit@3.3.0/", "lit-html": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/+esm", "lit-html/directive.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directive.js", "lit-html/directives/if-defined.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/if-defined.js", "lit-html/directives/class-map.js": "https://cdn.jsdelivr.net/npm/lit-html@3.3.0/directives/class-map.js", "@lit/reactive-element": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/+esm", "@lit/reactive-element/": "https://cdn.jsdelivr.net/npm/@lit/reactive-element@2.0.4/", "@m3e/web/core": "/node_modules/@m3e/web/dist/core.js", "@m3e/web/core/a11y": "/node_modules/@m3e/web/dist/core-a11y.js", "@m3e/web/core/anchoring": "/node_modules/@m3e/web/dist/core-anchoring.js", "@m3e/web/core/bidi": "/node_modules/@m3e/web/dist/core-bidi.js", "@m3e/web/core/layout": "/node_modules/@m3e/web/dist/core-layout.js", "@m3e/web/core/platform": "/node_modules/@m3e/web/dist/core-platform.js" } } </script>
to support enhanced editor tooling and developer experience.
To enable autocomplete and hover documentation, install the
extension. It will automatically detect the manifest bundled with this package and surface tag names, attributes, slots, and events in supported files.
Alternatively, you can explicitly reference the and in your workspace settings.
Welcome to M3E, a modern, from-scratch implementation of
. It provides a complete, spec-aligned component suite with dynamic color, expressive motion, and full accessibility.
Leverages Material 3's design tokens, dynamic color, shape and motion systems for highly customizable UI elements.
ESM-first with modular entry points and tree-shaking for minimal bundles.
Built-in support for accessibility standards, ensuring inclusive experiences for all users.
Effortlessly adapt components to your brand or user preferences with Material 3's advanced theming capabilities.
Lightweight, fast-loading components designed for modern web platforms.
Built with secure-by-default patterns, including XSS-safe templating, Trusted Types compatibility, and support for strong Content Security Policies (CSP).
